# Tick 2026-06-23 17:57 PT — 5/5 shipped

Theme: closing the note-hashtag loop in five orthogonal directions
(paste-time warning + discovery + per-clip promote + destructive
combo + shell-comment). Every feature this tick composes existing
pure primitives so the new entry points produce byte-identical
output to running the constituent actions back-to-back.

## Shipped

1. **`b0000a9` feat(palette): warning tint for rows whose note contains caution keywords**
   - New pure module `lib/note-warning.ts` (24 curated keywords)
   - In-page palette CSS: `.row.note-warn` warm-red tint + ⚠ glyph
   - hasNoteWarning / firstWarningKeyword / formatNoteWarningTooltip
   - 73/73 sanity (defensive, every keyword, word-boundary discipline,
     punctuation, multi-word edge cases, stateful regex safety,
     realistic notes, canonical lowercase)

2. **`da66486` feat(palette): Cmd+K "Find hashtags in notes"**
   - New pure module `lib/hashtag-discovery.ts`
   - Cmd+K Filter command: scans currentClips for distinct hashtags
     across notes, sorts desc by clipCount with alpha tiebreak
   - Composes extractHashtagsFromNote (single source of truth on
     hashtag grammar with bulk Tag-from-notes)
   - alreadyTagged per-entry flag distinguishes "would do work"
     from "already promoted everywhere"
   - 4-shape toast grammar; hint pre-computes topN=1 preview
   - 36/36 sanity (defensive, extraction, aggregation, alreadyTagged
     tracking, sort, topN, formatters, realistic 12-clip workspace)

3. **`45e8681` feat(detail-view): per-clip "Promote N #tags" chip**
   - New pure module `lib/note-hashtag-promote.ts`
   - Note-row foot chip: accent-tinted, visible when note has
     #hashtag tokens NOT in structured tag list
   - Live refresh on detailNote input + detailTags input/change
   - dataset.merged stash so click acts on plan user SAW
   - Composes existing tag-from-notes primitives so single + bulk
     paths produce byte-identical merged tag lists
   - 4-shape grammar; tooltip surfaces full pending + alreadyTagged
   - 47/47 sanity (defensive, extraction, case-insensitive, dup
     collapse, hyphens, non-array tags, merged order, all 3
     formatters, realistic chip cycle)

4. **`491bf7f` feat(bulk-bar): "Tag from notes + clear notes" combo**
   - New pure module `lib/tag-from-notes-clear.ts`
   - Eraser-icon button next to standalone Tag-from-notes
   - TARGETED clear: only wipes notes on clips where promotion
     happened (no-hashtag clips keep notes, all-already-tagged
     clips keep notes)
   - Pre-prompt destructive confirm
   - Composes existing extractHashtagsFromNote + mergedTagsForClip
     + sanitizeClipNote
   - 5-shape toast grammar; Cmd+K mirror in Bulk group
   - 39/39 sanity (defensive, per-clip action, plan aggregation,
     actionable, toast + button-title at every shape, realistic
     4-clip end-to-end + idempotent re-run)

5. **`901c2f5` feat(send-to): "Copy as cURL with note comment"**
   - New pure module `lib/curl-note-comment.ts`
   - Emit `curl 'url' # note` for link clips with notes
   - Critical safety: multi-line notes collapsed to single line
     (newline in `#` comment would terminate it)
   - sanitiseForShellComment caps at 200 chars + word-boundary
     truncation + C0 control strip
   - Composes existing curlCommandForClip + hasClipNote
   - New row id=curl-note slots between curl and fenced-code,
     send-to count 16→17
   - 46/46 sanity covering sanitiseForShellComment (defensive,
     cap, truncation), curlWithNoteCommentForClip (positive +
     negative), buildSendActions integration, shell-comment
     safety (backtick, dollar, hyphen-flag)

## Gates

- `tsc --noEmit` clean
- `npm run build` green for both chrome + firefox
  - popup.js: 374.0kb (+16.9kb vs 357.1kb baseline)
  - background.js: 48.0kb (unchanged)
  - content.js: 31.2kb (+2.8kb for note-warning module)
- All 85 sanity suites pass (80 existing + 5 new this tick)
- 241 new sanity checks (73 + 36 + 47 + 39 + 46)
- 0 regressions; only required updates: send-to + json-line row
  count assertions bumped 16→17

## Pushed

`git push origin main` succeeded: 8ccb44b..901c2f5 → origin/main
5 commits, each independently revertible.

## Deferred / open follow-ups

None deferred from the 5-target. Added 5 follow-up roadmap items
for next ticks:
- Per-site editable note-warning keyword list
- Cmd+K hashtag-discovery full panel UI (vs current toast)
- Detail promote-chip inline-tag-only toggle
- Combo split: promote+hashtag-clear vs promote+note-clear
- cURL-with-note settings option for line position
