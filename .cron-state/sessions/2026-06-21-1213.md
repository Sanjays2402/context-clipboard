# Tick 2026-06-21 12:13 PT — context-clipboard 5/5

## Features shipped (all on main, ~5 commits)

| # | Slice | SHA | Test |
|---|---|---|---|
| 1 | Send-to: keyboard nav in floating menu (↑↓/Home/End/Tab trap/type-ahead/Esc + auto-focus first row) | `9f74676` | exercised inline; no isolated sanity needed for pure DOM key handler |
| 2 | Send-to: remember last-picked action, float to top (lib/db meta row + pure `reorderSendActionsByLast` + `.send-row-recent` accent dot cue) | `09b0a07` | 18/18 send-to-reorder sanity |
| 3 | Send-to: "Copy as plain text (strip tokens)" row — template clips only, hidden on plain text/image/empty (pure `rawTextForClip`) | `31bef7a` | send-to sanity grew 87→94 |
| 4 | Privacy audit retention slider — Settings drop-down (10/30/60/100), live save + immediate trim + toast, replaces hard-coded 30 cap; respects on import-merge too | `66aabec` | 21/21 audit-retention sanity |
| 5 | Bulk-archive confirm previews first 3 clips with "+N more" tail (refactored into pure `src/lib/bulk-preview.ts` for reuse) | `c4da145` | 30/30 bulk-preview sanity |

## Quality gate

- `node_modules/.bin/tsc --noEmit` — green
- `npm run build` chrome + firefox — both green (popup 183.8 KB, background 44.5 KB, content 23.8 KB)
- 20 sanity suites — 456 total checks, all PASS
- 10 script tests — all PASS (redact-ui DB-version mismatch is the pre-existing playwright issue, unrelated)

## Code health

- 2 new pure lib modules: `lib/bulk-preview.ts`, plus extensions to `lib/send-to.ts` (`rawTextForClip`, `reorderSendActionsByLast`) and `lib/db.ts` (`getSendToLast`/`setSendToLast`/`trimPrivacyAuditToCap`). All leaf-level, no IO/DOM beyond db.ts's existing meta-tx pattern.
- `Settings` shape grew by one literal-union field (`privacyAuditRetention: 10|30|60|100`), additive — older imports default to 30 via `getPrivacyAuditCap`'s snap-to-valid logic.
- No new IDB schema bump (everything additive on existing `meta` store).

## Deferred

Nothing — 5/5 landed cleanly with full test coverage.

## STATE.md

- Marked 5 roadmap items shipped + added 8 new ones to keep queue at 15+ open items.
- Tick log entry appended at the top.
