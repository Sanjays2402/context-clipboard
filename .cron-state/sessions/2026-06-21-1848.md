# 2026-06-21 18:48 PT — 5/5 shipped

Branch: `main`.  Push: pending (this tick).

## Shipped

1. **`5c702c9`** feat(saved-searches): rename inline by double-clicking the chip label
   - New `renameSavedSearch(id, name)` in lib/db (case-insensitive collision math, preserves id+createdAt+query, allows case-only self-edits)
   - Popup: chip label swaps to `<input>` on dblclick, Enter/Esc/Tab/blur all funnel through single `commitSavedSearchRename` (no double-toast)
   - CSS `.saved-search-chip.renaming` accent glow + CSS.escape polyfill
   - Sanity 27/27

2. **`0915171`** feat(audit): right-click row to forget a single audit entry
   - New `removePrivacyAuditEntry(id)` in lib/db (scalpel companion to the nuke `clearPrivacyAudit`)
   - Every audit row carries `data-entry-id`; contextmenu confirm quotes the row's kind+subject so the user knows what's going
   - Re-renders panel; quick-chip state preserved
   - Sanity 26/26

3. **`f33d812`** feat(trash): Restore + pin combo button on trash rows
   - Icon-only second button (left of Restore pill), hidden when clip was already pinned
   - Click handler uses `target.closest("[data-act]")` so SVG-path clicks resolve
   - Toast carries an Undo that re-trashes both restore + pin in one shot
   - Sanity 9/9 (visibility math)

4. **`3281c91`** feat(search-history): pin a Recent query → promote to saved search
   - renderSearchHistory emits composite chip — apply button + opacity-0 pin button revealed on `:hover` / `:focus-within`
   - Right-click on the chip routes through the same `saveRecentAsSearch` helper
   - Just-saved query auto-migrates Recent → Saved on next render (existing dedup math handles it)
   - Sanity 14/14

5. **`d3b2207`** feat(site-rules): import/export the rule set as portable JSON
   - New pure `src/lib/site-rules-io.ts` (serializeRules + parseRulesJson + mergeRules, bounded MAX_RULES=200, never throws)
   - New `replaceSiteRules` in lib/db + background RPC for bulk-write
   - Popup: Export / Import header buttons, slide-in IO panel with merge/replace radio; Replace confirms before wiping; clipboard fallback chain (navigator → execCommand → manual)
   - Sanity 78/78 (serialize / parse defensive guards / merge math / replace math / immutability / round-trip)

## Deferred

None — all 5 shipped clean. None deferred.

## Quality gate

- `tsc --noEmit` — green
- `npm run build` — chrome 204.9 KB popup / 45.0 KB background / 23.8 KB content; firefox identical
- All 26 sanity suites pass — 668 total checks
- 16 script tests pass too (templates, export, lang-detect, retro-redact, site-rule-scrub, palette-host-boost, palette-last-query, redact, crypto, popup-encrypt)
- Pre-existing playwright redact-ui DB-version mismatch unrelated

## Roadmap refill (11 new items)

- Audit log: filter by clipId so clicking a clip's row in audit pre-filters to its action history
- Saved-search chip: drag-to-reorder so frequent ones float left
- In-page palette: "open sidepanel" affordance for tab-switching workflows (Chrome-only)
- Settings: per-kind retention split (text vs image, separate maxUnpinned)
- Trash row: "Restore everything from this host" — bulk-restore counterpart to forget-host
- Detail-view: per-clip retention overlay (TTL countdown banner when expiresAt is near)
- Bulk-bar: "Move to collection…" once collections ship (placeholder until then)
- Cmd+K palette: "Open my last saved search" (most-recent-applied)
- Site-rule form: paste a sample URL → auto-populate hostPattern field
- Audit panel: "Last 7 days" / "Last 30 days" filter alongside the bucket chips
- In-page palette: live token-counter when typing a {{template}} clip body
