# Tick 2026-06-20 03:27 PT — 5/5 shipped

Branch: feature/autoship  ·  Head: ad3b2a1

## Shipped (5 commits)

1. `72b7ed7` — feat(saved-searches): named queries as chips above search.
   New bookmark button on the search row + chip strip with apply/remove.
   Stored in IDB `meta` (no schema bump). Capped at 24, dedupes by name.

2. `97f584c` — feat(rules): per-site capture rules. Host or `*.example`
   patterns; effects = auto-tag / auto-pin / auto-redact / skip-capture.
   First match wins. Honored on copy-event + context-menu paths so
   skip-capture rules don't leak via right-click. Settings UI in the
   forget-host neighborhood. Rules apply on dedup-update too.

3. `7f37ed1` — feat(reveal): reveal-once mode for redacted clips. New eye
   button next to the redact shield (only shown when there's an original
   stored). Swaps detail body to original with a pulsing red banner +
   countdown; snaps back after 10s. DB never touched. Teardown on every
   exit path (timer, close, prev/next, manual re-click).

4. `656d25e` — feat(export-filter): pick what to export (pinned / tag /
   date range / skip images / redacted-only). Pure helper in
   `src/lib/export.ts`; sanity-tested with 11 cases. Live hint under
   the controls describes the active filter; success toast shows the
   subset count ("Exported JSON · 7 of 23 clips").

5. `ad3b2a1` — feat(count): result-count breakdown when filtering.
   Footer grows from "12 clips" to "12 clips · image · 8 text · 6 @github.com"
   when a filter is active, skipping bits that would be redundant with
   the filter itself. Capped at 3 breakdown bits.

## Gates

- `tsc --noEmit` — clean
- `npm run build` — chrome 75.9kb popup / 31kb bg / 15.8kb content;
  firefox same. Both targets green.
- 11/11 export filter sanity tests pass.

## Deferred

None. All five were genuinely solid slices.

## Roadmap refill

Topped up to ~16 items across search/capture/paste/privacy/data/UI.
Added new candidates: search highlight, search history ghost chips,
omnibox keyword, custom redaction patterns, anti-shoulder-surf blur,
bulk re-tag from current filter, keyboard cheatsheet overlay.
