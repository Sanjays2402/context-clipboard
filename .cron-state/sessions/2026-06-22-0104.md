# 2026-06-22 01:04 PT autoship tick

Shipped 5/5.

1. **`is:notemplate` search operator** (`795c401`) — inverse of `is:template`. New ParsedQuery.noTemplate bit; parser recognises "is:notemplate" (case-insensitive); applyQuery drops template clips. Empty-state hint grows op list; new Cmd+K "Hide templates" command. 24/24 sanity.
2. **Detail TTL countdown banner** (`9097d19`) — new `lib/ttl-banner.ts` with `computeTtlBanner(clip, now)` → expired/imminent(<1h soft-red)/soon(<24h accent)/hidden. Banner DOM above detail-body with Keep + Clear-TTL buttons. CSS tier classes. 34/34 sanity covers boundary edges (exactly 1h → soon, 24h → null, at-deadline → expired).
3. **Audit panel Download JSON** (`4bca469`) — new `lib/audit-export-json.ts`. Standalone privacy-receipt envelope (version=1, source, exportedAt, count, retention?, entries). Defensive entry cleanup. New "Download JSON" button between retention select + Clear. 33/33 sanity.
4. **Cmd+K "Show last forgotten host"** (`9bfabc5`) — new `lib/last-forgotten-host.ts` with `findLastForgottenHost(entries)`. Module-scope cache refreshed at boot + after every `runForgetHost` success. New palette command in Privacy group routes through `openSettings + restoreHostFromTrash`. One-tap rescue from the regretted bulk-action. 30/30 sanity.
5. **Bulk-bar storage delta** (`c6cf99f`) — new `lib/bulk-storage-delta.ts` with defensive `sumClipBytes` + `formatBytes` + `buildStorageDeltaLabel`. New `<span id="bulk-storage-delta">` in bulk-bar; computes `currentClips ∩ selectedIds` (visible-selected) with "of N shown" tail when selection spans beyond filter. 39/39 sanity.

## Gates
- `tsc --noEmit` ✅ clean
- chrome+firefox builds ✅ (popup 229.0KB +9.5, background 45.0KB, content 23.8KB)
- 35 sanity suites — 922 total checks pass
- 16 script tests pass

## Deferred
None — all 5 ran clean, no blockers.
