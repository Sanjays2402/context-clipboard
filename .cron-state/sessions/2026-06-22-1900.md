# Session 2026-06-22 19:00 PT

Cron tick — 5 features shipped cleanly to main + pushed.

## Shipped
1. **`19c38fc`** — `is:locked` search operator. New `lockedOnly` on
   ParsedQuery, parser branch (strict exact match), strict `===true`
   applyQuery gate, describeQuery surface, empty-state hint update,
   new Cmd+K "Show locked clips" command. 25/25 sanity.
2. **`7a75ad8`** — Bulk-bar lock/unlock selected. New padlock button
   with intent-adapting icon (lock vs lockOpen). New `db.setLocked(id,
   bool)` idempotent setter. New pure `lib/bulk-lock.ts` (4 helpers:
   intent decision, write count, toast, button title with skip count).
   bulkPin-style "if-all-then-undo" UX. New Cmd+K command. 42/42 sanity.
3. **`864fd69`** — Cmd+K "Lock all from active host". Companion to
   last tick's pin-from-host. New pure `lib/host-lock.ts` (4 helpers
   mirroring host-pin). Shared activeTabHost cache extended (no
   extra IDB read). Strict ===true skip for already-locked. 47/47
   sanity.
4. **`fb2020d`** — Send-to "Open in background tab". New row between
   incognito and site-search, chrome.tabs.create({active:false}) so
   tab loads without focus steal. New `urlForBackgroundTabOpen`
   builder + new SendAction kind `"bg-tab"`. Handler covers happy +
   fallback paths. send-to action count bumped 13→14 in both relevant
   sanity files; +13 new bg-tab specific checks.
5. **`1cacba4`** — Bulk-bar "Export selected as JSON". Cherry-pick
   downloads with importAll-compatible envelope. New pure
   `lib/bulk-export.ts` (4 generic-over-T helpers — envelope builder,
   JSON serialiser, filename composer, toast formatter). Send/arrow
   icon button. Handler fetches FULL ClipItem so all bits round-trip.
   New Cmd+K command. 63/63 sanity.

## Gates
- `tsc --noEmit` — clean.
- `npm run build` — chrome + firefox both succeed.
- All 56 sanity suites pass (1732 checks total, 190 new this tick).
- Build sizes: popup 287.0KB (+14.2), background 47.2KB (unchanged),
  content 26.9KB (unchanged).

## Deferred
None. All 5 features were genuinely solid — no padding, no scaffolding.

## Roadmap refill
Added 8 new items (lock-hotkey, bulk-open-similar-bg, lock+pin combo,
is:unlocked, bulk export tag filter, per-host lock-by-default, audit
lock undo, lock-since breadcrumb) on top of the carry-overs. Plenty
in the queue for the next tick.
