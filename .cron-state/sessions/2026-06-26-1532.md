# Tick 2026-06-26 15:32 PT — 5/5 shipped (frontend UX)

Theme: orthogonal frontend surfaces — image-viewer affordances, bulk-copy
receipts, a settings preview, and a calendar-day filter. Deliberately
away from the lang/wrap-override + note clusters of recent ticks.

## Shipped (5)

1. `2e17924` **feat(lightbox): save image to disk** — "Save image" control
   in the top-right cluster + `s` keyboard shortcut. Drops the clip's
   already-local data URL onto an `a[download]` and clicks it: zero
   network, zero permissions, MV3-clean. New pure `lib/image-download`
   owns the filesystem-safe filename (product stem + host slug + pixel
   dims + mime→extension), every segment gracefully omitted when its
   source is missing. New `download` icon. 26/26 sanity.

2. `59bf03c` **feat(lightbox): clickable position dot-strip** — carousel
   dots under the caption, one per image in the run, current one
   filled+pill-widened. Click a dot to jump straight to image K instead
   of stepping prev/next. Hidden for a lone image, wraps for long runs,
   honors prefers-reduced-motion, per-dot "Image K of M (current)"
   aria-label. Pure `lib/lightbox-dots`. 26/26 sanity.

3. `7079856` **feat(bulk-bar): char-total receipt** — both bulk Copy +
   Copy-as-Markdown completion toasts now echo the joined character total
   ("Copied 3 clips — 1,240 chars"), matching the button-hover preview
   the user already saw. Added `chars` accounting to the Markdown plan +
   a "(N chars)" button title for full parity. Code-point counted
   (emoji-safe), thousands-grouped, separators included. 51/51
   bulk-clipboard + 15/15 new bulk-md-chars sanity (updated the old toast
   assertions to the new format).

4. `611f682` **feat(settings): bulk-md separator preview swatch** — a live
   miniature under the "Copy as Markdown clip separator" select: two stub
   clips (a fenced one-liner + a cited blockquote) joined by exactly the
   seam the live bulk path emits, plus a one-line caption. Repaints on
   every change. Seam read from the SAME `bulkMarkdownSeparator` resolver
   the bulk copy uses, so the preview can't drift. Pure
   `lib/bulk-separator-preview`. 14/14 sanity.

5. `4303709` **feat(search): is:today operator + "Today" quick-chip** — the
   LOCAL calendar day (since local midnight), distinct from the rolling
   `after:24h`, matching the day-group dividers' "Today" bucket. Chip
   shows a live count, sits beside "Last 24h" so the calendar-vs-rolling
   distinction is discoverable. DST-correct midnight (re-zeroes the next
   calendar date, not +24h), threshold computed once at parse time. Cmd+K
   "Show today's clips" + empty-state hint. Pure `lib/today-filter`.
   20/20 today-filter + 13/13 is-today sanity.

## Deferred
- "Copy image to clipboard" detail button — `copyToClipboard` already
  writes images as a Blob via ClipboardItem, so a separate button would
  be redundant filler. Noted in STATE.md.

## Gate
- `tsc --noEmit` clean.
- chrome + firefox builds green (popup 468.3KB, background 48.0KB,
  content 31.2KB).
- 122/122 sanity suites pass (incl. the 4 new + the updated bulk-clipboard).
- Pushed `563aaa8..4303709` to origin/main, verified landed.

## New libs this tick
`image-download.ts`, `lightbox-dots.ts`, `bulk-separator-preview.ts`,
`today-filter.ts` — all pure, no DOM, all with dedicated sanity harnesses.
