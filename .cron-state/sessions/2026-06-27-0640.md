# Tick 2026-06-27 06:40 PT — 5/5 shipped (frontend UX)

Theme: push calendar buckets to the MONTH grain (the natural next step
the 02:09 tick flagged), extend the empty-bucket escape one rung wider,
and sharpen three long/abstract surfaces. Orthogonal spread: search /
empty-state / cheatsheet / list / settings.

## Shipped (5/5)
1. `06b8b7c` feat(search): is:thismonth + is:lastmonth operators + quick-chips
   - lib/today-filter: localMonthStart (zero time, snap day to 1st),
     localMonthEnd / localLastMonthStart (setMonth +/-1 off the snapped
     1st -> correct across 28/29/30/31-day months + DST-safe + year
     rollover), isThisMonth / isLastMonth both-ends predicates.
   - search.ts: thisMonthOnly (SUPERSET, inclusive lower only) +
     lastMonthOnly (bounded both ends, tiles against this-month);
     parse-time thresholds, applyQuery gates, describeQuery.
   - popup: chips counted independently (this-month shown only when
     thisMonthCount > thisWeekCount); Cmd+K, empty-state wall, cheatsheet.
   - 32/32 sanity-thismonth-filter.mjs.
2. `2cf0254` feat(empty-state): widen is:thisweek -> "Widen to this month"
   - lib/widen-bucket: WIDEN_MAP gains the week->month rung; each entry
     carries `fromLabel` so the "No clips from <bucket>" headline is
     data-driven (popup drops its today/yesterday ternary that couldn't
     name "this week"). is:thismonth + "last*" buckets don't widen.
   - 34/34 sanity-widen-bucket.mjs.
3. `91e2ced` feat(cheatsheet): live "N of M" match-count badge
   - lib/cheatsheet-filter: cheatsheetMatchLabel — "" filter-off, "6 of
     31", "No matches"; clamps matched to [0,total] + floors non-finite
     total so a bad caller can't render "40 of 31".
   - popup: tallied in the existing DOM walk; aria-live badge, hidden when
     label is "". CSS: absolute right-edge badge (tabular nums,
     pointer-events:none); :has() reserves input right-padding only while
     shown. 17/17 sanity-cheatsheet-match-count.mjs.
4. `0668400` feat(list): suppress redundant lone day-divider
   - lib/day-group: TODAY_HEADER / YESTERDAY_HEADER exported + used in
     dayLabel (single source of truth).
   - lib/redundant-divider (new, pure): isRedundantDayDivider gates to
     EXACTLY the lone day op + its matching divider label; compound /
     wider-bucket / "Pinned"-or-dated divider / nullish all keep it.
     First divider only. 23/23 sanity-redundant-divider.mjs.
5. `d9de909` feat(settings): density preview selected-row accent
   - lib/density-preview: DensityPreviewRow.selected; 2nd (text) stub row
     sets it. popup adds density-preview-row--selected; CSS mirrors
     .clip.selected (accent-soft + inset 1px outline). 20/20 sanity.

## Gate
- tsc --noEmit: clean.
- npm run build: chrome + firefox green (popup 485.2KB, background 48.0KB,
  content 31.2KB).
- New-feature sanity: 126/126 (32+34+17+23+20).
- Regression: thisweek 22, lastweek 21, today 20, yesterday 20,
  cheatsheet-filter 18 all pass.
- Pushed dfb6f82..d9de909 to origin/main, verified landed.

## Deferred
None — all 5 are solid, demoable, revertible slices.

## Notes for next tick
- Roadmap refilled (2026-06-27 06:40 PT block, 17 fresh frontend items).
- Natural next: the coarse "This week / Last week / Earlier" day-group
  rollup for the long older tail (now strongly motivated by 6 calendar
  operators); a quick-chip scroll-page chevron (the strip carries 6
  calendar chips + hosts now and overflows); the long-standing "Copy
  line N" detail gutter + recent-searches-on-focus dropdown, both
  untouched by the recent calendar cluster.
