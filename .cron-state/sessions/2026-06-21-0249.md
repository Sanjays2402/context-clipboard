# 2026-06-21 02:49 PT — tick log

5/5 shipped. All five commits on `main` and pushed to origin.

## Features (in commit order)

1. `76b3301` — feat(list): compact-row mode
   - Settings toggle + body class + CSS overrides. ~36px rows for dense
     scanning of long lists. Pure CSS, no data dropped.
   - Cmd+K palette toggle ("Compact rows / Expand row spacing") flips the
     same bit.

2. `b3a7e22` — feat(dedup): review duplicates panel
   - New `findDuplicateGroups()` + `mergeDuplicateGroup(hash)` helpers in
     lib/db. Sibling of the existing bulk `mergeDuplicatesByHash` but
     listing-first so users can inspect each group before collapsing.
   - New `#dupes-panel` modal, per-group Merge buttons, Merge-all button,
     Cmd+K "Review duplicates…" command. Global Esc handler updated.
   - 15/15 sanity tests pass (.cron-state/sanity-find-dupes.mjs).

3. `12ad8cc` — feat(archive): clip archive mode
   - Additive `archived?: boolean` on ClipItem (no IDB bump).
   - Search parser learns `is:archived` with flip semantics: default
     filter HIDES archived; operator surfaces archive-only.
   - `toggleArchive(id)` bumps lastSeenAt on un-archive.
   - Detail-view toolbar gets Archive/Inbox button. Quick-chip strip
     gets "Archived" pill with count. Clip rows render archived badge.
   - New Phosphor-style archive + inbox icons in lib/icons.
   - 11/11 sanity tests pass (.cron-state/sanity-archive.mjs).

4. `a1f32fa` — feat(privacy): audit log
   - Ring buffer of last 30 privacy actions in meta store. 11 verbs:
     redact, unredact, scrub-origin, retro-redact, forget-host, set-ttl,
     clear-ttl, archive, unarchive, trash, restore.
   - Audit writes hooked into all six existing privacy call sites in
     popup.ts (fire-and-forget; failures never block the user).
   - Settings panel gets a new "Privacy audit" section with colour-coded
     rows + Clear button. Cmd+K "Show privacy audit log" scrolls to it.
   - 15/15 sanity tests pass (.cron-state/sanity-privacy-audit.mjs).

5. `b6c5fc9` — feat(notes): inline composer
   - Replaces bare `prompt("Add a quick note:")` with a real dialog.
   - Multi-line textarea (Cmd/Ctrl+Enter saves), inline tag input,
     quick-tag chip strip (top tags minus noise auto-tags, click toggles
     them into the input), "Pin this note" checkbox.
   - Background `addNote` action extended to accept `tags?` + `pinned?`
     and apply them after ingest so dedup + auto-tag still fire.

## Gate

- `tsc --noEmit` — clean
- `npm run build` — chrome + firefox both green
  - popup 157.3KB · background 40.2KB · content 23.8KB
- Full sanity suite (every `.cron-state/sanity-*.mjs` + `scripts/test-*.mjs`):
  - archive 11/11, find-dupes 15/15, privacy-audit 15/15, palette 11/11,
    highlight 9/9, sort 13/13, jump 14/14, pattern-hits 23/23, merge-dupes
    25/25, import-dedup 14/14, export 11/11, retro-redact 9/9, lang-detect
    22/22, host-boost 8/8, palette-last-query 9/9, scrub 16/16,
    templates 11/11. All pass.
- Pre-existing playwright `test-redact-ui.mjs` DB-version mismatch
  unrelated (carried over from earlier ticks).

## State updates

- STATE.md "Shipped" list grew by 5; matching roadmap items marked
  done. Roadmap topped up with 8 fresh items (bulk archive, archive
  empty state, audit filter chips, audit export, note tab-context tags,
  saved-search auto-import, recent-host strip, archive-chip hide-when-
  active).
- STATE.md branch line corrected: working branch is `main`, not the
  old `feature/autoship`.
