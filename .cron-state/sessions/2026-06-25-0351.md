# Session 2026-06-25 03:51 PT

Batch tick — 5/5 shipped. Frontend focus override in effect (all slices UX).
Continued last tick's "fresh non-hashtag frontend" cluster; 3 of 5 were
direct follow-ups to features that landed 2026-06-24, 2 new gaps.

## Shipped (in order, each its own commit)

1. `7c06937` feat(detail-stats): click the content-stats breadcrumb to copy the summary
   - New pure helpers in lib/content-stats: contentStatsClipboard (delegates
     to formatContentStats so copied text == rendered line) +
     formatContentStatsCopyToast (echoes summary, caps long ones).
   - renderContentStats marks row data-copyable + cursor/title; click handler
     re-reads open clip, recomputes WYSIWYG payload. CSS hover/active states.
   - No-op for hidden/empty/image states.

2. `5a5ef7a` feat(bulk-bar): Copy selected as Markdown
   - New pure lib/bulk-markdown: planBulkMarkdown renders each clip with the
     single-clip MD grammar (image ![](), link [](), code fenced via
     detectCodeLang, text cited blockquote), joins with `---` rule. Templates
     RAW (un-expanded). Imports detectCodeLang from lib/util.
   - New monochrome `markdown` glyph in lib/icons.
   - Wired: bulk-bar button, updateBulkBar title refresh (visible-slice
     preview), Cmd+K "Copy selected as Markdown" command. Visible-list order.

3. `01ee6b4` feat(list): Shift+Up/Down keyboard range-extend
   - New moveActiveWithRange() reuses lib/range-select helpers (computeRange/
     idsForRange/rangeIdsToAdd) — same additive extend-only contract as
     Shift+Click: first Shift+Arrow seeds anchor + selects origin, each step
     adds anchor->cursor span, anchor stays put, no-op at edges.
   - Shift branch only outside search box. Footer hint + cheatsheet updated.

4. `c35cd17` feat(footer): keyboard-focus position breadcrumb (row N of M)
   - New pure lib/focus-position: formatFocusPosition (1-based, clamps stale
     index into range, null for empty/un-clampable).
   - listKeyboardActive flag set by Arrow handlers, cleared on search focus.
     renderFocusPosition called from render(). Accent pill, aria-live=polite.

5. `246019c` feat(quick-chips): scroll-shadow edge-fade affordance on overflow
   - New pure lib/scroll-shadow: computeScrollEdges (scrollLeft/scrollWidth/
     clientWidth -> {start,end}, 1px epsilon, non-overflow => no fades).
   - refreshQuickChipsScrollShadow toggles data-shadow-start/end; CSS fades
     via mask-image. Re-measured on rAF after chip render + passive scroll
     listener + window resize.

## Gate (once, end of batch)
- `tsc --noEmit` — clean (EXIT 0)
- `npm run build` — chrome + firefox both green, popup 409.4kb

## Push
- 300c638..246019c -> origin/main, verified via git log origin/main.

## Notes / deferred
- Nothing deferred — all 5 genuinely solid + independently revertible.
- New libs: content-stats (extended), bulk-markdown, focus-position,
  scroll-shadow. All pure, zero network, MV3 + local-only intact.
- Roadmap still has 10 open items in the current frontend block (search
  recent-dropdown, word-wrap per-clip override, copy-line-N, hover-peek
  tooltip, code soft-highlight, density radio, prev/next wrap-around,
  sticky day headers, chip-style tag editor, count-aware copy label) —
  above the <5 refill threshold, so no refill this tick. Added 5 fresh
  follow-ups spawned by today's work.
- No test runner exists (package.json has no test script); gates are
  tsc + build only, as the prompt specifies.
