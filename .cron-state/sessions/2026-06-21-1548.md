# 2026-06-21 15:48 PT — ctxclip tick

Target 5, shipped 5.

## Shipped

1. **fabfb98** — `feat(audit): roll up by day - Today/Yesterday/older with counts and fold`
   - New `src/lib/audit-rollup.ts` (pure, ~140 LOC): `groupAuditByDay`
     buckets by local-day key, `labelForDay` returns Today/Yesterday/
     "Wed, May 18" (or with year when it differs), `totalAuditEntries`
     convenience. Local-day boundaries (not UTC) so late-tz midnight
     entries don't split across two groups.
   - Popup `renderAudit` split into thin filter + new
     `renderAuditGroupsHtml` emitting `<div.audit-day>` + `<button.audit-day-head>`
     headers with chevron + label + count. Per-day collapse in
     `Map<key, bool>` override (NOT IDB — collapse is a glance,
     resets on popup close).
   - `auditList` click handler grows leading day-head toggle branch.
   - CSS: uppercase tracked headers, accent-tinted chevron when open,
     hover/focus-visible feedback, indented rows.
   - 36/36 audit-rollup sanity (empty/today/mixed/sort-desc-on-shuffled/
     midnight-boundary/year-rollover-label/totalAuditEntries/single-
     entry/labelForDay probes).

2. **40a581c** — `feat(send-to): Copy URL only - bare page URL for one-click share`
   - New pure `urlOnlyForClip(c)` in `lib/send-to.ts`: link clips
     return `c.content` (the body IS the URL), text/image clips return
     `c.source.url`, both validate http(s). Undefined for
     scrubbed/empty-link/non-http(s) schemes (data:/file:/chrome:/
     mailto:)/whitespace.
   - New `SendAction id="url-only"` between md-link and fenced-code so
     copy-variants cluster reads md-link / url-only / fenced-code /
     raw-text / json.
   - Distinct from md-link (`[title](url)` for notes) and open-source
     (navigates) — this is the bare URL for paste into chat / search.
   - send-to sanity grew to 111/111 with 17 new url-only cases.

3. **081603f** — `feat(site-rules): show "last matched X ago" alongside clip-count badge`
   - New `usagesForRules(rules, clips)` in `lib/db.ts` returns
     `Map<id, {count, lastMatchedAt}>` in one scan (max of
     `c.lastSeenAt` across attributable clips, same first-match-wins
     as `countClipsForRules`).
   - Kept `countClipsForRules` untouched (backwards-compat / tested);
     new helper is opt-in.
   - `renderSiteRules` switches: each badge now shows
     `12 clips · 3d ago` with softer italic tail. Hover title pops
     full timestamp.
   - Drops the now-unused `countClipsForRules` import from popup.ts.
   - CSS `.rule-usage-ago` (60% opacity italic, opacity bump on hover).
   - rule-count sanity grew to 28/28 with 15 new usagesForRules cases
     (multi-clip max math, empty-rules/empty-clips, absent-when-zero,
     no-host skip, first-match-wins parity, max-not-last-iterated).

4. **edeed31** — `feat(notes): composer remembers "Pin this note" per-session`
   - New module-scope `notePinSticky` bool. `openNoteComposer` reads
     it into the checkbox (replaces hard-coded false).
   - `saveNoteFromComposer` stamps the bit on save (happy path);
     pin-checkbox `change` listener stamps on every explicit toggle
     so a check-then-cancel cycle still sticks (intent matters even
     when draft was abandoned).
   - Per spec: NO IDB write. Popup-close is the reset boundary
     (matches the audit day-collapse model from the same tick).
   - No new sanity test — pure UI state with no extractable logic.

5. **b73a934** — `feat(archive): distinct archive empty-state + drop redundant chip`
   - `renderQuickChips` wraps the Archived push in a
     `hasOp("is:archived")` guard so the chip stays out of the strip
     while the user is already in archive view (avoids a stealth
     "exit" pill that visually looks identical to filter chips).
   - `render()` branches on `parsed.archivedOnly` when `currentClips`
     is empty — distinct copy ("No archived clips yet." vs
     "...match this filter.") + new `<button.empty-action>`
     "Show daily list" pill that strips `is:archived` from the
     search box (whitespace-bound regex so adjacent ops survive).
   - `listEl` click handler grows leading exit-archive branch.
   - CSS `.empty.archive-empty` (softer text-secondary tone) +
     `.empty-action` (pill-shaped, accent-tinted hover,
     focus-visible ring).
   - No new sanity test — render-path branching + chip-visibility
     guard covered by tsc + existing archive sanity.

## Quality gates

- `tsc --noEmit` — clean.
- `npm run build` — green for both chrome + firefox.
  - popup 188.7 KB (+4.9 vs last tick)
  - background 44.5 KB
  - content 23.8 KB
- All 21 sanity suites pass — **514 total checks**.
  (11 archive + 20 audit-export + 30 audit-filter + 21 audit-
  retention + 36 audit-rollup + 30 bulk-preview + 32 context-tags
  + 11 export + 15 find-dupes + 9 highlight + 27 history-export
  + 14 import-dedup + 14 jump + 25 merge-dupes + 11 palette + 23
  pattern-hits + 15 privacy-audit + 28 rule-count + 18
  send-to-reorder + 111 send-to + 13 sort)
- 16 script tests pass too (templates + export + lang-detect +
  retro-redact + site-rule-scrub + palette-host-boost + palette-
  last-query + redact + crypto + popup-encrypt).
- Pre-existing playwright redact-ui DB-version mismatch is unrelated.

## Deferred / nothing dropped

No features deferred. All 5 shipped clean.
