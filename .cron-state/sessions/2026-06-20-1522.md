# 2026-06-20 15:22 PT — autoship tick

Branch: `feature/autoship`
Result: 5/5 shipped, tsc + chrome/firefox builds green.

## Features

1. **Tag all filtered** — `7fcbae8`
   `tagAllFiltered()` palette action. Union-merge across the visible
   window (filter IS the selection). Idempotent skip when every clip
   already carries every requested tag. 25-clip confirm guard mirrors
   pin-all. Pure popup wiring.

2. **Per-site custom redaction patterns** — `a2bc0af`
   `customPatterns: string[]` added to SiteRule. Background ingest
   applies them AFTER built-in PII redaction (text clips only) via the
   new `applyCustomPatterns(content, patterns)` helper in util.ts.
   Bounded: 32 patterns / 200 chars each. Invalid patterns rejected at
   save time with line-number feedback. Settings panel grew a monospace
   textarea inside the rule form; rule rows get a `regex ×N` badge
   with full-pattern tooltip. 15/15 sanity tests pass against the
   bundled util.ts (literal/multi/invalid/cap/case-insensitive/
   no-match/long-skip/empty-skip/multi-match-per-pattern).

3. **Image re-fetch from source URL** — `8688224`
   New `refetchImage` RPC. Service worker re-fetches the source URL
   (nearbyText fallback to source.url), re-runs createImageBitmap for
   dimensions, updates content/mime/bytes/width/height/lastSeenAt.
   Detail view swaps the image + meta row in place without re-opening
   the panel. Button only renders for clips with an http(s) source.
   Spinner CSS animation while inflight. New Phosphor-style refresh
   icon. Failure modes surface as explicit error toasts.

4. **Collapsible long nearby context** — `d161fe7`
   Nearby paragraphs >360 chars collapse with a "Show more (+N)"
   toggle. Full text cached in `data-full` so the toggle is a pure DOM
   swap. Truncation snaps to the last word boundary. Expanded view
   caps at 280px before scrolling. "Show less" recomputes from cache.
   CSS adds `.nearby.collapsible`, `.nearby-toggle`, `.nearby-text`.

5. **Right-click "Paste from Context Clipboard"** — `757c95e`
   Fourth contextMenu entry with `editable` scope. Click forwards
   the in-page palette via the same `cc-open-palette` message the
   keyboard shortcut uses (single renderer). content.ts `pick()` now
   detects a focused editable field and inserts the text directly
   (via setFieldValue + input/change events), instead of only
   round-tripping the system clipboard. Image clips still go via
   clipboard. skipCapture rules don't block this menu (it's about
   paste-out, not ingest). chrome:// / about: tabs silently skipped.

## Quality gates

- `tsc --noEmit` — clean
- `npm run build` — chrome + firefox both green, esbuild done in ~12ms
- `applyCustomPatterns` sanity — 15/15 pass

## Deferred

None — all 5 features genuinely solid before the gate.

## Notes for next tick

- Roadmap thinned of the just-shipped items; added "Per-site rule edit
  (click row to load form)" and "Custom-pattern test field" as natural
  follow-ups to the redaction work.
- Image re-fetch sets up "image auto-recapture: rule-based scheduled
  re-fetch for tracked images" as a logical next slice.
- Right-click paste sets up "in-page palette: filter by kind" since the
  overlay now matters more outside the popup.
