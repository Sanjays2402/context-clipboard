# Tick 2026-06-27 02:09 PT — 5/5 shipped (frontend UX)

Theme: push calendar buckets up to the WEEK grain + two discoverability
fixes on long surfaces. All 5 frontend, orthogonal spread.

## Shipped (5/5)
1. `36bf541` feat(search): is:thisweek operator + "This week" quick-chip
   - lib/today-filter: WEEK_START_DAY (Monday/ISO, single flip-point) +
     localWeekStart / localWeekEnd / isThisWeek (DST-safe via setDate).
   - SUPERSET of today/yesterday — chip counted independently, shown
     only when thisWeekCount > todayCount.
   - search.ts: parse-time threshold + applyQuery + describeQuery.
     popup: chip, Cmd+K, empty-state hint, cheatsheet row.
   - 22/22 sanity-thisweek-filter.mjs.
2. `27867de` feat(search): is:lastweek operator + "Last week" quick-chip
   - localLastWeekStart + isLastWeek; bounded both ends, tiles against
     this-week (mutually exclusive by the shared this-week-Monday bound).
   - Chip (counted independently, shown when last week has clips), Cmd+K,
     empty-state, cheatsheet. 21/21 sanity-lastweek-filter.mjs.
3. `d350f42` feat(empty-state): "Widen to this week" chip on empty day bucket
   - New pure lib/widen-bucket: widenSuggestion gates to EXACTLY a lone
     day-bucket query (compound queries left alone), case-insensitive,
     returns rewritten query + label.
   - data-act=widen-bucket handler. 22/22 sanity-widen-bucket.mjs.
4. `6d2b784` feat(cheatsheet): live shortcut-filter input
   - New pure lib/cheatsheet-filter: cheatsheetRowMatches (case-
     insensitive substring over combined row text). Empty groups hide;
     "no matches" note. Opens cleared + focused; `?`-to-close suppressed
     while input focused, Esc always closes. 18/18 sanity.
5. `8d8cb22` feat(settings): image stub row in density preview
   - density-preview.ts: optional `image` flag + 4th IMAGE stub. Popup
     renders a thumb (mono glyph); CSS shrinks 42->28px at compact,
     mirroring the real list. 15/15 sanity-density-preview.mjs.

## Gate
- tsc --noEmit: clean.
- npm run build: chrome + firefox green (popup 480.5KB, background
  48.0KB, content 31.2KB).
- New-feature sanity: 98/98 (22+21+22+18+15).
- Pushed 931d5a2..8d8cb22 to origin/main, verified landed.

## Deferred
None — all 5 are solid, demoable, revertible slices.

## Notes for next tick
- Roadmap refilled (2026-06-27 02:09 PT block, 17 fresh frontend items).
- Natural next: is:thismonth/is:lastmonth (extends the same parse-time-
  threshold pattern + widen-bucket WIDEN_MAP week->month), and the
  coarse "This week / Last week / Earlier" day-group dividers that pair
  with the new week operators.
