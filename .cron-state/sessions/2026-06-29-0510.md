# Tick 2026-06-29 05:10 PT — 5/5 shipped (frontend UX)

Theme: extend the code/prose/tabular content-classification cluster across
new UI surfaces. All 5 frontend, all building on the same shared predicates
so the filter, the badges, the chips, and the send-to rows can never drift.

## Shipped (5/5)

1. **780671a** — `is:csv` / `is:tsv` search operators.
   New `lib/table-kind.ts` (csvMatches/tsvMatches) reuses the
   `looksLikeTableRow` gate the Copy-as-CSV/TSV send-to rows use. The pair
   partitions the tabular set (tab -> tsv, comma-only -> csv, never both).
   Wired: ParsedQuery flags + defaults + parse branch + applyQuery gate +
   describeQuery bits, 2 Cmd+K Filter commands, empty-state operator wall,
   ? cheatsheet. Partition logic spot-checked 8/8.

2. **c6eb8ba** — Detail meta code/prose badge, click to filter.
   New `lib/clip-kind-badge.ts` reuses codeMatches/proseMatches (same
   predicates behind is:code/is:prose + the quick-chip counts). Click closes
   detail + toggles the matching operator. Code tints accent, prose neutral.
   HTML + element ref + renderClipKindBadge in openDetail + click handler + CSS.

3. **2e5e189** — List-row faint mono left-stripe on code clips.
   `.clip.is-code::before` 2px accent stripe; row already position:relative.
   Same codeMatches predicate; low-alpha at rest, firms on hover/active.

4. **2401634** — Bulk-bar "Copy selected as CSV".
   New `lib/bulk-csv.ts` joins tabular clips into one multi-row CSV
   (byte-identical to per-clip csvRowForClip stacked). Skips non-tabular
   honestly, char+byte receipt matches Copy/Copy-as-Markdown, hides when
   nothing tabular. New monochrome `table` glyph in lib/icons. Join +
   RFC-4180 quoting spot-checked (3 rows / 2 skipped / quoted cell).

5. **94af3e9** — Empty-state complement suggestion.
   New `lib/complement-suggestion.ts`: lone is:code empty + prose exists ->
   one-tap "Show prose clips (N)" (and reverse). Gated to lone operator,
   only when complement non-empty. Counts from the wide window (quick-chip
   tallies). New swap-complement empty-action handler. Decision 8/8.

## Gate
- `node_modules/.bin/tsc --noEmit` — clean.
- `npm run build` — chrome (popup 534.9kb) + firefox both green.

## Pushed
- origin/main fed98d3 -> 94af3e9 (verified via git log origin/main).

## Deferred
- None this tick — all 5 genuinely solid, no padding.

## Notes for next tick
- Top roadmap section refilled with 18 fresh items (2026-06-29 05:10 PT
  refill): tabular cluster branches — CSV/TSV quick-chips, mini-table
  preview, column-count stat, is:tabular union operator, bulk TSV,
  line-level ops, language-in-badge, etc.
- The LSP diagnostics surfaced mid-edit on search.ts/popup.ts were STALE
  (transient broken-state snapshots) — the authoritative `tsc --noEmit`
  was clean after each. Don't trust the inline LSP mid-multi-edit; gate
  on tsc.
