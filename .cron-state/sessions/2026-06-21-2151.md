# 2026-06-21 21:51 PT — autoship tick

**Shipped: 5/5 features straight to `main`.**

## Features

1. **Audit panel: clip + time-window scope filters** (`7af49fe`)
   - Alt-click any jumpable audit row → pivots to clip-scope mode (instead of jumping); pill above chips reads `clip: <preview>` × clear.
   - New `<select id="audit-window">` All / 7d / 30d in audit header.
   - Three-axis filter stack: window → clip → bucket. Each pass narrows; auto-scope-away when combo lands on zero rows.
   - Summary shows "N of M" whenever any scope is active.
   - Settings open resets all three filters (glance, not preference).
   - Bundles two roadmap items (clipId filter + time-window) into one commit because the scope-banner UI is shared.

2. **Saved-search chips: drag-to-reorder** (`4361a8e`)
   - HTML5 native DnD, no library. Four strip-level listeners (dragstart/dragover/dragleave + drop + dragend) handle the whole lifecycle.
   - Dragging chip dims + tilts 1.5deg; insertion hint paints 3px accent inset on drop target's left/right edge.
   - Rename-mode chips opt out (`draggable` only when not renaming); del button + rename input preventDefault dragstart.
   - New `reorderSavedSearches(orderedIds)` in lib/db: defensive (missing ids tail-append, unknown ignored, dupes collapse, no-op no-write when order matches).
   - 29/29 sanity covers swap math + tail preservation + unknown pruning + dupe collapse + no-op + 4-entry round-trip.

3. **Trash: bulk-restore strip** (`b5ca001`)
   - New pure `src/lib/trash-host-rollup.ts` — `groupTrashByHost(trash, minCount=2)` defaults to 2 so singles don't get redundant chips. Sort by count desc, then newestDeletedAt desc, then alpha.
   - New `restoreAllFromHost(host)` in lib/db: symmetric counterpart to forgetHost with same www-strip + lowercase normalisation.
   - Popup paints up to 6 chips above trash row list; one click confirms (above 5) and bulk-restores. Honest partial-success messaging.
   - 27/27 sanity covers empty/pair bucketing + www-strip + sort math + no-url skip + custom minCount + 50-row count + loose-shape tolerance.

4. **Cmd+K: "Open my last saved search"** (`24676b8`)
   - New `LAST_SAVED_SEARCH_KEY` meta row (64-char id cap, trimmed + null-safe).
   - In-memory mirror `lastSavedSearchId` refreshed at popup boot + every apply path so palette open doesn't pay an IDB read.
   - Stamped BEFORE render (fire-and-forget). Cleared synchronously when the underlying chip is deleted.
   - Palette command label adapts: `Open last saved search · Github issues` when recent, greyed-out unavailable when nothing applied yet.
   - 15/15 sanity covers default-empty + round-trip + trim + cap + multi-write last-wins + null/undefined coercion + meta isolation against send-to-last + palette-last-q.

5. **Site-rule form: paste a URL → auto-fill host + wildcard suggest** (`e0d41dd`)
   - New pure `src/lib/host-pattern.ts` — `looksLikeUrl()` + `extractHostPattern()`.
   - Paste handler on rule-host input preventDefaults URL-shape pastes, replaces with extracted host, fires synthetic input event so downstream sees it.
   - Wildcard suggestion chip below input: `Try wildcard: *.github.com` for 3+ label hosts. One click swaps it in; self-hides on next render.
   - loadRuleIntoForm triggers suggest after loading so mid-edit promotion is one click. resetRuleForm clears it.
   - Defensive: rejects data: / file: / chrome: / about: / javascript: / view-source: / blob: / moz-extension: schemes so accidental data-URL paste doesn't land "data" as the rule target.
   - 55/55 sanity covers looksLikeUrl shape detection + URL extraction (subdomain math + query/hash + PSL + deep subdomain + uppercase + protocol-relative + www-strip + port + auth + IPv4 + trailing dot + plain-text fallthrough + non-host scheme rejection).

## Gate

- `node_modules/.bin/tsc --noEmit` — green.
- `npm run build` — chrome + firefox both succeeded. Sizes:
  - popup 219.5KB (+14.6 vs last tick — audit scope + drag-reorder + trash host strip + last-saved palette + paste handler + suggest chip)
  - background 45.0KB
  - content 23.8KB
- All 31 sanity suites pass (794 total checks).
- All 10 script tests pass (test-redact-ui's pre-existing playwright DB-version mismatch is unrelated and predates this tick by weeks).

## Pushed to origin/main

```
e0d41dd feat(site-rules): paste a URL into host input - auto-extract + wildcard suggest
24676b8 feat(palette): "Open my last saved search" - recall the most-recent chip
b5ca001 feat(trash): bulk-restore strip - one-click reversal of forget-host
4361a8e feat(saved-searches): drag-to-reorder chips so frequent ones float left
7af49fe feat(audit): clip + time-window scope filters for the audit panel
```

## Notes

- Refilled the 18:48 PT roadmap section (5 items shipped this tick out of 11 — drained from 6 to 0 of the originally-fresh items, plus the time-window which was bundled into the audit-scope commit). Added 11 fresh items in a "2026-06-21 21:51 PT refill" block covering detail-view TTL countdown, palette token-counter, sidepanel affordance, per-kind retention split, rule form "test against active tab", `is:notemplate`, detail-view "Show audit history" jumper, bulk-bar tag picker, recent-strip drag-reorder, last-forgotten-host palette command, and audit-only JSON export.
- Bundled clip-scope + time-window into one commit (`7af49fe`) because the scope-banner UI is shared between them — splitting would have led to two adjacent commits both touching the same audit panel render path with overlapping diff hunks.
