# Tick 2026-06-25 18:59 PT — 5/5 shipped (frontend UX, five surfaces)

Frontend override still active. Five orthogonal slices across five
distinct surfaces; four were direct follow-ups blessed in last tick's
open list, deliberately spread (not one cluster).

## Shipped (5/5)
1. `4f427bd` feat(detail): keyboard nav + Backspace/Delete for tag chips
   - Roving-tabindex toolbar (WAI-ARIA): ←/→/Home/End move focus between
     chips; Backspace/Delete removes the focused chip AND lands focus on
     a neighbour (slid-in tag / new last / raw input when emptied).
   - New pure lib/tag-chip-nav (nextChipFocusIndex clamps at ends,
     focusIndexAfterRemove resolves landing, isChipNavKey/isChipRemoveKey).
   - Click × + keyboard both route through one removeDetailTag helper so
     they can't drift. focus-visible ring (keyboard-only). 23/23.
2. `fcc4316` feat(search): is:wrapoverride operator
   - Surfaces clips pinned to their own detail-body wrap (the "what did I
     override?" review pass the per-clip wrap feature lacked an entry to).
   - Gate = wrap-pref.hasWrapOverride, SAME predicate the detail toggle
     badges with → filter + paint agree. Direction-agnostic (presence).
   - parser + applyQuery + describeQuery + Cmd+K live-count command
     (greys at 0) + empty-state hint. 8/8.
3. `641e474` feat(list): richer hover-peek for link clips
   - Folds source title + full URL into the tooltip even when the body
     fits the row, so two same-host links disambiguate in one hover.
   - New lib/linkPeekTooltip dedups against visible row text + collapses
     title/url/body duplicates; per-field flatten + cap + ellipsis. 11/11.
4. `ac18536` feat(detail): tok-punct token class in code tinting
   - Soft-tints structural glyphs { } ( ) [ ] ; , + arrows => -> so
     nesting/call structure is scannable. Per-theme muted slate var.
   - Only runs in plain-text gaps (strings/comments claimed first → never
     mis-tinted), arrows lead the alternation so => is one span, config
     langs (yaml/toml/ini) opt out via spec.punct=false, XSS-safe. 18/18.
5. `ca50138` feat(detail): per-clip force-language override
   - Detail dropdown to override the auto-detected tinting language or
     force tinting off (for a clip detectCodeLang false-positives), with
     a live "auto → Rust" hint so Auto isn't a black box.
   - New pure lib/lang-override (3-state effectiveLang folds language +
     whether-to-tint into one verdict; normalizers; LANG_OPTIONS) +
     additive ClipItem.langOverride + db.setLangOverride (mirrors
     wrapOverride). Hidden for images + while searching. 25/25.

## Gate
- `tsc --noEmit`: clean
- `npm run build`: chrome + firefox both green (popup 437.5kb)
- Pushed 367e972..a76c8bb → origin/main, verified.

## Sanity
- 83 checks across 5 new bundler-free suites saved to .cron-state:
  tag-chip-nav 23, lang-override 25, link-peek 10, is-wrapoverride 10,
  code-punct 15. All green standalone.
- Pure modules also verified against the REAL source via throwaway
  esbuild harnesses during the build (23/8/11/18/25); temp files removed.

## Notes / deferred
- Nothing deferred — all 5 genuinely solid + independently revertible.
- Two new additive ClipItem fields this tick: none — only langOverride
  (wrapOverride already shipped). Round-trips through import/export
  untouched (spreads preserve unknown fields).
- 10 fresh follow-ups added (tag-chip DnD, is:langoverride, force-lang →
  export wiring, tok-punct opt-out toggle, day-header count badge, etc).
