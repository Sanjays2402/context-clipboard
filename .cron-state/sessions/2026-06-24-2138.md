# Tick 2026-06-24 21:38 PT — 5/5 shipped

Theme: deliberately broke out of the multi-tick hashtag/note-cleanup
rut (last ~6 ticks were all hashtag plumbing). Picked five fresh,
orthogonal frontend slices filling obvious UX gaps a real user would
hit, each in a different surface (detail body, list selection, search
field, bulk bar, detail header).

## Shipped

1. **`1c06003` feat(detail-view): content-stats breadcrumb**
   - New pure module lib/content-stats.ts
   - "1,240 chars · 198 words · 12 lines" footnote under the body
   - Code-point chars (astral glyphs count 1), whitespace words,
     CRLF/CR-normalised lines; drops the line seg for single-line
     bodies; hides for image/empty clips
   - 53/53 sanity: counting core, word-split edges, line
     normalisation, emoji code-points, kind gating, thousands
     grouping (zero/neg/NaN/Inf/float), grammar, JSON/prose/CSV e2e

2. **`4682392` feat(list): Shift+Click range selection**
   - New pure module lib/range-select.ts
   - Direction-agnostic span from anchor (last explicit toggle) to
     clicked row; add-only (never deselects); anchor stays put for
     re-extend (Finder/Gmail)
   - Gated on active selection (or Cmd/Ctrl) so the long-standing
     Shift+Click=copy-as-Markdown shortcut survives
   - Keyboard X-toggle moves the anchor too; resets on clear
   - 37/37 sanity: both directions, bounds clamp, no-anchor collapse,
     degenerate lists, float trunc, id projection stale-skip, add-only
     idempotence, two-extend e2e

3. **`49862e5` feat(search): inline clear (×) + Esc-to-clear**
   - Clear button shows only when the box has content; synced on
     input AND in render() so programmatic value changes stay honest
   - Esc handled explicitly (not flaky native type=search clear);
     falls through to global handler when box is empty
   - Suppressed native webkit cancel glyph; muted styling
   - DOM-coupled UI slice, verified at the build gate

4. **`1e36355` feat(bulk-bar): Copy selected clips as text**
   - New pure module lib/bulk-clipboard.ts
   - Joins text/link bodies with "\n\n"; visible list order; skips
     images (reports skip count in toast); raw (un-expanded) templates;
     trailing-trim with internal formatting preserved
   - Button title + Cmd+K label preview copyable count
   - 43/43 sanity: join sep, image skip+count, all-images empty, raw
     templates, trim+preserve, empty-body skip, defensive nullish,
     toast+title grammar, mixed e2e + determinism

5. **`d267c36` feat(detail-view): word-wrap toggle**
   - New meta key detail_wrap (getDetailWrap/setDetailWrap in db.ts),
     mirrors list_sort persistence; default-on, explicit-false-only flip
   - .nowrap modifier → horizontal scroll for tabular/log/wide code;
     button hidden for images; accent when wrap-off
   - New textWrap icon; Cmd+K command available only while a wrappable
     clip is open
   - 20/20 sanity on the persistence coercion contract

## Gates

- `tsc --noEmit` clean
- `npm run build` green for chrome + firefox
  - popup.js: 401.3kb (+9.6kb vs 391.7kb baseline, 4 new modules +
    wiring + 1 icon)
  - background.js: 48.0kb (unchanged)
  - content.js: 31.2kb (unchanged — no content-script touches)
- New sanity: 153/153 (53 + 37 + 43 + 20). Feature 3 is DOM-coupled
  (no pure module), verified at the build gate.

## Pushed

`git push origin main`: 8b0152f..d267c36 → origin/main, verified.
5 commits, each independently revertible.

## Deferred / open follow-ups

None deferred from the 5-target. Refilled the roadmap with 19 fresh
non-hashtag frontend items (content-stats copy, keyboard range-extend,
recent-searches-on-focus dropdown, bulk copy-as-MD, per-clip wrap
override, line-N copy, hover-peek tooltip, soft code highlighting,
density radio, focus breadcrumb, prev/next wrap-around, sticky
day-group headers, chip-shadow overflow, chip-style tag editor, etc.)
plus 5 direct follow-ups (anchor visual hint, selection-text stat,
bulk-copy order toggle, wrap keyboard shortcut, clear-button fade).
