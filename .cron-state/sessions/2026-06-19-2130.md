# tick 2026-06-19 21:30 PT (first tick)

**Branch:** `feature/autoship` (created off `main` this tick)
**Pushed:** yes — `origin/feature/autoship` HEAD = `76fb6e7`
**Gates:** tsc clean ✓ · build chrome+firefox clean ✓ · 15/15 search sanity tests ✓
**Shipped:** 5/5

## Commits (oldest → newest)

| SHA | Slice |
|---|---|
| `baf3c5a` | chore(cron): bootstrap STATE.md roadmap |
| `c407d53` | smart search operators (`kind:` `host:` `tag:` `is:` `before:` `after:`) |
| `1a306bc` | soft-delete trash (7-day retention, restore UI, opportunistic GC) |
| `9183ea8` | quick-filter pill row above tag chips |
| `47f75d9` | export as Markdown + CSV (JSON + encryption unchanged) |
| `76fb6e7` | image width/height + byte-size capture, shown in list/detail/export |

## What changed by area

- **`src/lib/search.ts`** (new) — pure parser + applier for inline operators.
- **`src/lib/export.ts`** (new) — Markdown + CSV serializers.
- **`src/lib/db.ts`** — DB_VERSION 3→4 (additive `trash` store); added
  `trashClip` / `restoreClip` / `listTrash` / `emptyTrash` / `trashCount` /
  `purgeOldTrash`.
- **`src/lib/types.ts`** — `ClipItem.width?` + `ClipItem.height?`.
- **`src/background.ts`** — added `imageDims()` via `createImageBitmap`
  (works in SW), opportunistic trash GC at end of every ingest.
- **`src/popup/popup.{ts,html,css}`** — search-box uses `parseQuery/applyQuery`;
  Trash section in settings; quick-filter pill row; export-format dropdown
  + Markdown/CSV branches; image thumb overlay + detail "Image" meta row.
- **`scripts/test-export.mjs`** (new) — exercises serializers via esbuild.

## Defers / notes

- Recent-host quick filter strip in the roadmap is now partly covered by
  the new quick chips (top 3 hosts). Left it open because the explicit
  "host strip" idea was bigger (top 5 with hit counts inline). Will pick
  it up later if needed.
- Tesseract OCR is still on the disabled-since-v0.3.1 path; OCR-related
  features in the new code (`is:ocr` chip, "OCR text" row in CSV/MD) work
  against existing `ocrText` data but new captures don't fill it. Not
  this tick's job to revive Tesseract — that's its own slice.
- Deliberately did NOT add tests-runner / CI; Sanjay's roadmap was about
  features. The ad-hoc `scripts/test-search.mjs` was kept off-disk
  (temp), and `scripts/test-export.mjs` is the only persisted check.

## Lock

Released at end of tick.
