# 2026-06-23 13:46 PT — 5/5 shipped

Theme: note-family composition + host-rule operator family completion.
Built on last tick's `is:hostlocked` + Copy-note-as-Markdown by
adding the natural composition/parity follow-ups across 5
orthogonal vectors.

## Shipped

1. **`is:hostpinned` / `is:hostredacted` / `is:hostscrubbed`**
   `aba7302` — host-rule operator family parity with last tick's
   `is:hostlocked`. New pure module `lib/host-rule-flags.ts` generic
   over `HostRuleFlag` (`"autoPin"|"autoRedact"|"autoScrubOrigin"`).
   Same cross-store join shape, same first-match-wins semantics,
   same per-host verdict cache. Three new Cmd+K Filter commands +
   3 new ParsedQuery flags + 3 new applyQuery predicates with
   fall-open contract. 29/29 sanity.

2. **Detail send-to "Copy clip + note as Markdown"** `c4cdc32` —
   composite fenced-code + blockquote in a single copy for the
   "share snippet + caveat together" workflow. New pure module
   `lib/clip-note-markdown.ts` composes existing `fencedCodeForClip`
   + `noteAsMarkdownBlockquote` (composition law verified by sanity
   test 12). Hides when either body or note missing. Send-to row
   count: 15→16 (both `sanity-send-to` and `sanity-json-line`
   assertions updated). 19/19 sanity.

3. **`is:notenewer:Nd` / `is:noteolder:Nd`** `15d6b9a` — chronology
   gates over `noteUpdatedAt` with `Nd`/`Nh`/`Nw` duration grammar
   delegating to existing `parseDuration`. Distinct from `after:`
   (re-copy recency) — same divergence rationale as recently-noted.
   Band-pass via AND-semantics composition. Both imply `is:noted` +
   finite stamp. Strict typo rejection. 2 new Cmd+K commands.
   33/33 sanity.

4. **Bulk-bar "Tag from notes"** `e4f50da` — extract `#hashtag`
   tokens from selected clips' notes and merge into structured tag
   list. Promotes inline note-style tagging to `tag:` searchable
   structure. New pure module `lib/tag-from-notes.ts` with strict
   regex (SOL/whitespace/punct leader, 32-char tag cap, 16-tag note
   cap, case-fold, hyphens allowed mid-tag). 8-shape toast grammar
   with failure-mode disambiguation. New `hash`-icon button + Cmd+K
   mirror; button hides when nothing to do. 53/53 sanity.

5. **In-page palette: surface clip notes inline** `bb510ec` —
   italicised tail under preview line so user sees caveat
   ("staging only") BEFORE pasting. New pure module
   `lib/palette-note-tail.ts` (trim + whitespace-collapse + 80-char
   word-boundary truncation with hard-slice fallback). Shadow-DOM
   `.note-tail` CSS (italic dim gold #c4a86b, "note: " prefix).
   `PaletteClip.note?` field added (back-compat). Both background.ts
   `cc-open-palette` send sites pass through `note: c.note`. 24/24
   sanity.

## Quality gates

- `tsc --noEmit` clean
- chrome+firefox builds green
- popup: 357.1kb (+19kb for 5 features + 5 new pure modules + new
  icon + tag-from-notes button + .note-tail CSS in content)
- background: 48.0kb (+0.6 for note pass-through)
- content: 28.4kb (+1.5 for note-tail rendering + import)
- All 80 sanity suites pass (5 new this tick = 158 new checks; 0
  regressions across existing 75 suites)

## SHAs

```
bb510ec feat(palette): in-page palette surfaces clip notes inline
e4f50da feat(bulk-bar): Tag from notes - extract #hashtags into structured tag list
15d6b9a feat(search): is:notenewer:Nd / is:noteolder:Nd - note chronology operators
c4cdc32 feat(send-to): Copy clip + note as Markdown - combined fenced-code + blockquote
aba7302 feat(search): is:hostpinned / is:hostredacted / is:hostscrubbed - host-rule operator family
```

## Deferred / not attempted

None this tick. All 5 features genuinely landed solid.
