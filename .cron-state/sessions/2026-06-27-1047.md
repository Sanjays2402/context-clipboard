# Tick 2026-06-27 10:47 PT — 5/5 shipped (frontend UX)

Five distinct surfaces, deliberately spread so no cluster dominates.
Four new pure lib modules; 27 headless assertions all green;
`tsc --noEmit` clean; chrome + firefox builds succeed. Pushed to
`origin/main` (a3c6446..2b2ee6b), verified landed.

## Shipped

1. **`f564a4e` feat(settings): density preview adds a pinned-tier stub row**
   - `lib/density-preview.ts`: added `pinned?` to `DensityPreviewRow`
     (distinct from `selected`); leading stub row sets it.
   - popup render emits `.density-preview-row--pinned` + a filled pin
     glyph; CSS mirrors `.clip.pinned` (accent-soft sweep gradient) and
     shrinks the glyph at compact. The swatch now covers the pinned-tier
     look at each grain, not just resting/selected/image.

2. **`890a142` feat(detail): warm "captured <relative>" breadcrumb**
   - New pure `lib/capture-relative.ts` (`captureRelative` +
     `captureRelativeLabel`) reuses the is:today/yesterday/thisweek/
     lastweek/thismonth/lastmonth predicates. Narrowest-bucket-first →
     just now / earlier today / yesterday / earlier this week / last week
     / earlier this month / last month; "" for older clips (hidden).
   - popup renders a muted accent subtitle line under the exact datetime
     (`#detail-captured-rel`), hidden when the phrase is empty.

3. **`d0a49b4` feat(cheatsheet): ArrowDown from filter → roving row focus**
   - New pure `lib/cheatsheet-nav.ts` (`cheatsheetRowNav`): down-from-
     input→row0, down/up rove, up-off-top→input, no-wrap at bottom,
     stale-index re-enters top, empty-set no-op.
   - popup: keydown on the filter input + delegated keydown on the card;
     `visibleCheatsheetRows()` collects non-hidden rows; rows take
     `tabindex=-1` on demand; `.cheatsheet-row:focus` accent ring + fill.

4. **`19a00c0` feat(lightbox): windowed "…" edge cues become PAGE buttons**
   - `lib/lightbox-dots.ts`: new `dotStripPageTarget` steps the active
     image one window-width (`DEFAULT_MAX_DOTS`) in the clicked direction
     and clamps; returns null at a dead edge / unknown active / lone image.
   - popup: the `…` spans are now `<button data-page>` with hover/focus +
     aria-label; the dots click handler pages on a `.lightbox-dot-more`
     hit. CSS resets the button chrome to keep the bare "…" look.

5. **`2b2ee6b` feat(quick-chips): click-to-page chevrons on faded edges**
   - `lib/scroll-shadow.ts`: new `pageScrollTarget` (+`PAGE_SCROLL_FRACTION`
     0.8) — target scrollLeft one page toward an edge, clamped; no-op on
     non-overflow / bad metrics.
   - HTML wraps `#quick-chips` in `.quick-chips-wrap` with back/forward
     chevron buttons; popup toggles them in lockstep with the edge fades
     in `refreshQuickChipsScrollShadow`, `pageQuickChips` smooth-scrolls.
     CSS positions them over the faded edges with a backdrop blur.

## Verification

- `node verify-tmp.mjs` (esbuild-bundled the four new pure modules,
  ran 27 assertions): ALL PASS. Covered calendar-bucket ordering +
  just-now threshold, dot-strip page clamps/null edges, scroll page
  clamp + overlap, cheatsheet nav edges incl. stale index. Temp script
  removed after.
- `node_modules/.bin/tsc --noEmit` → exit 0.
- `npm run build` → chrome + firefox both built (popup.js 490.7kb).

## Deferred / not done

- None deferred from the picked five. Plenty of open roadmap items remain
  (>5), so no refill this tick. Notable still-open frontend gaps for next
  ticks: detail "Copy line N" gutter, recent-searches dropdown on focus,
  lightbox thumbnail filmstrip, coarse "Earlier" day-divider rollup,
  "Display & export" settings subheading.
