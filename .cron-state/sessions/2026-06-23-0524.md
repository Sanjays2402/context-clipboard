# Tick 2026-06-23 05:24 PT — context-clipboard autoship

**Status:** 5/5 shipped. tsc + chrome+firefox build green.

**Theme:** full per-clip note-family completion — every feature
extends the note primitives (`note?: string`, `hasClipNote()`,
`sanitizeClipNote()`) shipped 1 tick ago into a coherent 5-piece
suite.

## Shipped

1. `0cd3b12` **feat(search): is:nonoted** — strict-complement parity
   twin of `is:noted`. Three surfaces (filter, detail-view Clear
   visibility, future recently-noted gate) share the same
   hasClipNote() predicate; the two operators partition the clip
   space (AND is empty by construction). 18/18 sanity.

2. `721a67a` **feat(detail): noteUpdatedAt breadcrumb** — schema-
   additive stamp written by setClipNote on every value-changing
   write (no-op fast path skip; clears on note-delete so re-noted
   starts fresh). New pure note-updated-since module (5-tier
   formatter with calendar-day boundary math, clock-skew clamp).
   Detail-view paints a "Noted X ago" pill in the note-row foot
   with full YYYY-MM-DD HH:MM tooltip. saveDetailNote() repaints
   immediately on save/clear. 25/25 sanity.

3. `634a3b1` **feat(palette): Cmd+K "Show recently noted"** —
   chronology companion to recently-locked for the note family.
   Pure recently-noted module mirrors recently-locked exactly
   (7d window, newest-first sort, label grammar 0/1/many with
   freshest-age hint, available:false greys row at 0). Run handler
   appends `is:noted` since search bar can't express
   `noteUpdatedAt >= now - 7d` directly; count + hint above tells
   the precise chronology scope. 24/24 sanity.

4. `a52fe7f` **feat(trash): hover-preview surfaces trashed clip's
   own note** — closes the loop between last-tick's trash-row
   hover-match + last-tick's note feature. When a trashed clip
   carries a note, the tooltip's tail surfaces it as
   "Note: <summary>" — single highest-signal context at
   trash-housekeeping time. formatTrashRecaptureTooltip extended
   with optional `trashed` arg (back-compat: omit = original
   2-shape tooltip). 80-char cap, word-boundary truncation,
   newlines collapse. Composes with both head shapes (match vs
   no-match). 32/32 sanity (22 existing + 10 new).

5. `ea1b1e4` **feat(bulk-bar): Add note to selection** — overwrite-
   and-warn semantics (notes are prose so merge would yield
   franken-text). New pure bulk-note module: planBulkNote projects
   {created, replaced, cleared, unchanged, finalValue} via the
   SAME sanitizeClipNote() the editor uses (single source of
   truth). 6-shape toast grammar + 4-shape button-title hover
   adapt to selection mix. Pre-prompt label + post-action toast
   both surface the replace-count so consequences are visible.
   Empty input clears (mirrors detail save-empty); prompt-cancel
   (null) distinct from prompt-empty (""). Cmd+K mirror in Bulk
   group. Idempotent re-run → all-unchanged, no IDB writes.
   34/34 sanity.

## Quality gates

```
node_modules/.bin/tsc --noEmit          # clean
npm run build                           # chrome + firefox both green
                                        # 320.9kb popup / 47.4kb bg / 26.9kb content
```

All 68 sanity test files still pass after the changes.

## Deferred

Nothing was deferred — all 5 shipped solid. Roadmap refilled with
12 new ideas, several extending the note family further (`is:
hostlocked`, "Note all from active host", per-kind note
auto-capture, `is:noteshorter:N`/`is:notelonger:N`, "Tag from
notes" hashtag extraction).

## Files touched

```
src/lib/search.ts                    +28 lines  (is:nonoted)
src/lib/types.ts                     +27 lines  (noteUpdatedAt field doc)
src/lib/db.ts                        +5 lines   (setClipNote stamping)
src/lib/note-updated-since.ts        new file (~125 lines)
src/lib/recently-noted.ts            new file (~180 lines)
src/lib/trash-match.ts               +60 lines  (note tail)
src/lib/bulk-note.ts                 new file (~200 lines)
src/popup/popup.ts                   +180 lines (5 features wired)
src/popup/popup.html                 +2 lines   (stamp pill + bulk-note btn)
src/popup/popup.css                  +20 lines  (stamp pill styles)
.cron-state/sanity-is-nonoted.mjs    new (18 tests)
.cron-state/sanity-note-updated-since.mjs   new (25 tests)
.cron-state/sanity-recently-noted.mjs       new (24 tests)
.cron-state/sanity-trash-match.mjs   rewritten (32 tests, was 22)
.cron-state/sanity-bulk-note.mjs     new (34 tests)
```

133 new sanity tests this tick.
