# Tick 2026-06-25 13:50 PT — 5/5 shipped (frontend UX, fresh surfaces)

Frontend override still active. Deliberately picked five orthogonal UX
gaps across different surfaces (list / detail body / detail tags /
detail code / settings) instead of extending one cluster.

## Shipped (5/5)
1. `67a410a` feat(list): sticky day-group headers for time-ordered clip lists
   - Today / Yesterday / weekday / dated dividers before each calendar-day
     run; only for recent/oldest sorts; pinned tier collapses to one
     "Pinned" header. Pure lib/day-group (local-time, DST-safe). Interleaved
     non-.clip header divs — all .closest handlers + data-idx nav ignore them.
2. `4656712` feat(detail): per-clip word-wrap override
   - Plain click = sticky per-clip override (wins over global); Alt-click
     clears to follow global; accent dot telegraph. Additive `wrapOverride`
     field + db.setWrapOverride. Global default toggle moved to Cmd+K. Pure
     lib/wrap-pref.
3. `e773272` feat(detail): chip-style tag editor
   - Each tag a pill with × (one-click remove); raw input stays add/edit
     surface; chips mirror live across input/change/openDetail/note-promote.
     Pure lib/tag-chips (parse/remove/dedupe, case-insensitive).
4. `e55228d` feat(detail): soft syntax tinting for detected code clips
   - Strings/comments/keywords/numbers tinted when no search needle active;
     search-match still wins while searching. Dependency-free lib/code-highlight,
     single master regex so keywords inside strings aren't mis-tinted. Escapes
     before spanning — XSS-safe. 19 runtime checks pass. Per-theme token vars.
5. `138c3a2` feat(settings): row-density control with new cozy tier
   - Comfortable / Cozy / Compact select replacing the lone compact checkbox.
     density field mirrored to legacy compactRows bool (palette toggle +
     import/export keep working); old settings migrate via resolveDensity.
     13 runtime checks pass. New Cmd+K cycle-density command. Pure lib/density.

## Gate
- `tsc --noEmit`: clean
- `npm run build`: chrome + firefox both green (427.4kb popup)
- Pushed 64a13d2..138c3a2 → origin/main, verified.

## Notes / deferred
- Nothing deferred — all 5 genuinely solid + independently revertible.
- Verified two pure modules (code-highlight, density) with throwaway esbuild
  runtime harnesses (XSS safety + migration precedence); temp files removed.
- New per-clip `wrapOverride` field is additive + round-trips through
  import/export untouched (settings/clip spreads preserve unknown fields).
- 9 fresh follow-ups added to STATE.md (keyboard tag-chip removal, code
  operator token class, force-language override, per-window density, etc).
