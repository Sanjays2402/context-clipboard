# Session 2026-06-27 21:02 PT — 5/5 shipped (frontend UX)

Batch tick. Closed five open loops from the 15:36 tick's follow-up list,
spread across five orthogonal surfaces. All frontend per the standing
override. Each = a pure module + a thin popup wire.

## Shipped (5/5)
1. `08af953` feat(bulk-bar): byte weight on Copy-as-Markdown hover + toast.
   planBulkMarkdown gains a `bytes` field (utf8ByteLength over the joined
   doc), reusing lib/bulk-clipboard's helpers so MD + plain copy report
   identical char+byte figures. Formatters updated; no popup wiring change.
2. `d3d1ead` feat(trash): float about-to-purge rows to the top.
   New lib/trash-sort floats imminent(<24h)+expired rows ahead of the
   newest-first bulk (stable partition), sorted most-urgent-first. Wired
   into renderTrash BEFORE the 50-row slice. Delegates urgency to
   lib/trash-ttl so sort == tint.
3. `2ab83d0` feat(search): warm all-clear empty-state for is:expired.
   New lib/empty-reassurance + .empty-reassure CSS (success tint). Gated
   to the lone operator; REASSURANCE_MAP is the extension point.
4. `0fa2a89` feat(detail): actionable rescue copy on the expired TTL banner.
   ttl-banner expired tier now "Expired N ago" + "Pin to keep it —
   otherwise the GC sweeps it at the next capture" (points at the existing
   Keep button). Copy + doc only.
5. `86d2d18` feat(note-composer): live char-counter tail vs the 2,000 cap.
   New shared lib/note-count (N/cap label + strictly-over-cap), wired into
   the composer foot + #note-char-count span + .note-char-count CSS.
   Mirrors .detail-note-count; same CLIP_NOTE_MAX_LEN + UTF-16 units the
   sanitizer slices on.

## Gate
- tsc --noEmit: clean.
- npm run build: chrome + firefox green (popup 500.0KB, bg 48.0KB, content 31.2KB).
- 30/30 headless sanity over the 4 new pure modules (esbuild→ESM, in-repo
  temp script, removed after): trash-sort float+stable-tail+purity+malformed;
  empty-reassurance gating (lone vs compound vs other ops); note-count
  over-cap boundary (2000 ok, 2001 over) + comma grouping + null-safe;
  bulk-markdown bytes>=chars on multi-byte (ö) + empty-plan + toast/hover tails.

## Deferred
None — all 5 solid.

## New pure modules
- src/lib/trash-sort.ts
- src/lib/empty-reassurance.ts
- src/lib/note-count.ts
(bulk-markdown.ts extended, not new.)

## Push
b447301..86d2d18 → origin/main, verified via git fetch + log.
