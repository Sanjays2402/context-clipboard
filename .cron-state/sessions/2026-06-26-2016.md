# Session 2026-06-26 20:16 PT — 5/5 shipped (frontend UX)

Batch tick. All five are frontend/UX slices per Sanjay's standing
override. Worked directly on main, one commit per slice, gated once at
the end, pushed 2d185da..d071cfa.

## Shipped
1. `0cfc3eb` feat(search): `is:yesterday` operator + "Yesterday" quick-chip
   - lib/today-filter: localYesterdayStart (DST-safe, steps calendar date
     back one + re-zeroes) + isYesterday both-ends predicate sharing the
     today-midnight boundary with isToday (mutually exclusive by design).
   - search.ts: yesterdayOnly flag + yesterdayAfter/yesterdayBefore window
     at parse time; applyQuery two-bound gate; describeQuery "yesterday".
   - popup: "Yesterday" quick-chip (live count, after Today), Cmd+K
     "Show yesterday's clips", empty-state hint.
   - 20/20 sanity-yesterday-filter (bounds, disjoint-from-today, defensive).

2. `1f574c8` feat(lightbox): window the position dot-strip for long runs
   - lib/lightbox-dots: windowLightboxDots (sliding band centred on the
     active dot, clamped so it's always in-range + active always inside)
     + dotWindowLabel ("N of M"). DEFAULT_MAX_DOTS = 15.
   - popup renderLightboxDots: windowed band + aria-hidden "…" edge cues +
     count label (only when windowed). Short runs unchanged.
   - CSS: .lightbox-dot-more + .lightbox-dot-count.
   - 27/27 sanity-lightbox-dots-window.

3. `e204cb1` feat(bulk-bar): byte-size receipt on Export-selected toast
   - lib/bulk-export: formatExportBytes + utf8ByteLength (TextEncoder +
     surrogate-aware fallback) + shared appendBytesTail; both toast paths
     take optional bytes (absent/0 omits the tail; counts comma-group).
   - popup: measure UTF-8 size of exactly the JSON written, thread it in.
   - 87/87 sanity-bulk-export (+30: byte tiers, multibyte/emoji, parity).

4. `4994bc1` feat(settings): live preview for the Row-density control
   - lib/density-preview: pure model — densityPreviewRows (fixed rows),
     densityPreviewClass (SCOPED class, never the global body class) +
     densityPreviewCaption.
   - popup renderDensityPreview wired to settings-open + select change.
   - CSS: .density-preview tiers (comfortable/cozy/compact + tag hidden).
   - 17/17 sanity-density-preview.

5. `d071cfa` feat(cheatsheet): "Image viewer" key group + calendar ops row
   - popup.html: new "Image viewer" group (6 keys: Enter/Space, [ ]/←→,
     +/-, 0, S, Esc) after "In detail view"; is:today/is:yesterday row in
     Search operators. HTML/CSS only; overlay already scrolls.

## Gate
- tsc --noEmit: clean
- npm run build: chrome + firefox green (popup 474.2KB, bg 48.0KB, content 31.2KB)
- 125/125 sanity suites pass (3 new this tick + extended bulk-export)

## Deferred
None — all five are solid, demoable user-facing slices.

## Invariants held
Local-only (no network added — image save/export are local data URL /
in-memory Blob), MV3 intact, dark-first design language, no emoji in
extension chrome / commits.
