# 2026-06-22 05:03 PT — ctxclip batch tick

**Shipped 5/5.**

1. `542f879` feat(detail): "Show audit history" jumper
   - New icon button on detail header pivots privacy audit panel
     to clip-scope mode (mirror of alt-click from audit row,
     but starting from the clip side).
   - New pure `src/lib/detail-audit-jump.ts` (precheck +
     describe helpers).
   - New `history` icon in icons.ts.
   - `refreshDetailHistoryTitle` runs on every openDetail to
     populate the tooltip with live match-count.
   - 24/24 sanity.

2. `100ec2a` feat(palette): "Jump to next archived clip"
   - Cycles archived clips newest-first (wraps), opens
     detail-view for each next clip — daily list filter stays.
   - New pure `src/lib/next-archived.ts`
     (archivedClipsSorted + nextArchivedClipId +
     describeArchiveCycle).
   - Popup caches archivedCount in render() (cheap, filters over
     already-loaded `wide` slice); palette command in Navigate
     group; jumpToNextArchived() re-reads listClips at click time.
   - 34/34 sanity.

3. `a8f7f79` feat(send-to): "Copy as table row"
   - TSV/CSV body → "| col | col | col |" Markdown row.
   - New pure `src/lib/table-row.ts` with looksLikeTableRow +
     splitTableCells + escapeCell + tableRowForClip.
   - Wired between raw-text and json in buildSendActions.
   - 49/49 table-row + 112/112 send-to sanity (one count
     updated: 10 → 11 actions).

4. `632f11d` feat(history): Recent chips drag-to-reorder
   - HTML5 native DnD mirroring the saved-searches strip;
     promote frequent queries left without committing as full
     saved-search.
   - New lib/db.ts `reorderSearchHistory` with defensive race
     handling (unknown queries pruned, dupes collapsed, missing
     tail-append; no-op + 0 writes when order unchanged).
   - Four strip-level handlers + CSS for dragging/drop hints.
   - 24/24 sanity.

5. `fe5b273` feat(palette): in-page palette ⌥⏎ copies URL only
   - New modifier dispatches PickMode "url-only" — always
     system clipboard, never direct-paste (user reached for ⌥⏎
     specifically because they want it on their clipboard).
   - New closure helpers PickMode + urlOnlyFor (http(s)-gated,
     trimmed, defensive against null/data:/file:/chrome:/about:/
     javascript:/bare-host).
   - Hint footer updated.
   - 29/29 sanity.

## Gates
- `tsc --noEmit` → green
- `npm run build` (chrome+firefox) → green
- All 39 sanity suites pass — 1051 total checks
  - new this tick: 24 detail-audit-jump + 34 next-archived +
    49 table-row + 24 reorder-search-history + 29 palette-url-only
  - updated this tick: 112 send-to (was 111, +1 for table-row
    order assertion)

## Deferred
None — all 5 features cleared the quality bar.

## Push
`git push origin main` — verified `git log --oneline origin/main`
on next fetch.
