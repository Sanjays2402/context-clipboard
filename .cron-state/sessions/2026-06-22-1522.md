# 2026-06-22 15:22 PT — autoship tick (5/5)

Shipped 5 vertical slices, each as its own commit:

1. `f27a649` feat(search): is:link operator — parity twin of kind:link
2. `812b47a` feat(send-to): Copy as cURL — one-line `curl '...'` for any clip with http(s) URL
3. `2d75a6f` feat(palette): Open in side panel — Chrome-only affordance in the in-page palette
4. `fe54bb9` feat(lock): per-clip "ask before deleting" lock — confirm-gate independent of pin
5. `9a13dd4` feat(palette): Pin every clip from active tab's host — one-shot triage command

## Gate

- `tsc --noEmit` clean
- `npm run build` green (chrome + firefox), both 272.8 KB popup
- All 52 sanity suites pass, 1512 total checks (198 new this tick)
- Pushed all 5 commits to `origin/main`

## Mid-tick fix

The send-to `acts.length` assertion in `sanity-json-line.mjs` was 12;
the new curl row bumped the count to 13. Test was updated alongside
the curl commit (folded in via `git rebase --autosquash` so slice 2's
gate stays green on its own — bisectable).

## Deferred

None — all 5 slices landed cleanly. STATE.md refilled with 19 new
candidates for the next tick (including companions to today's lock
slice: bulk lock/unlock, `is:locked` operator, "lock all from host").

## Notes for next tick

- `host-pin.ts` mirrors `clip-lock.ts` strict-bool semantics; if we
  add more "feature bits" on ClipItem, the strict gate is now the
  established pattern (no truthy fallthrough).
- The `openSidePanel` RPC handler's probe-mode pattern is a clean
  template for any future Chrome-only feature detection from a
  content script — re-use the `{probe: true}` convention.
- `trashWithLockGuard` now wraps the 5 user-initiated delete paths.
  Any new delete entry point (e.g. future "bulk move to collection")
  should route through it, not `trashWithUndo` directly. The bare
  `trashWithUndo` stays for confirmed flows (restore undo, audit
  jump-and-restore) where the user already crossed the bar.
