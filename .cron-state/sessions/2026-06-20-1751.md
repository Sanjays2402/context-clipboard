# 2026-06-20 17:51 PT — tick log

5/5 shipped. Clean tsc + chrome/firefox builds. All sanity suites green.

## Features

1. **`737e947` feat(rules): edit a per-site rule by clicking its row**
   - HTML: form-head with title + Cancel pill; row gains `title="Click to edit"`
   - CSS: edit-mode highlight on form + active row; cursor-pointer hover
   - TS: `editingRuleId` state, `loadRuleIntoForm` + `resetRuleForm` helpers;
     `addSiteRuleFromForm` now passes `id` so `upsertSiteRule` updates in
     place; deleting the rule currently being edited also resets the form
   - No schema change; backend's upsertSiteRule already supported id-based
     updates

2. **`0d473fc` feat(in-page palette): kind-filter chips, pinned-first ordering, shift+enter markdown**
   - One coordinated overhaul of the content-script palette covering
     three roadmap items (kind-filter, pinned priority, shift+enter MD)
     because they all touch the same render path
   - Background sends `pinned` on PaletteClip (both code paths)
   - Content script renders chips with live counts + 1-4 keyboard
     shortcuts when query is empty; pinned floats up with accent pin-dot
     + dashed divider; image rows dim when launch target is editable;
     Shift+Enter / Shift+Click pastes as markdown (text → fenced block
     if multiline, link → MD link, image → ![alt](url))
   - Hint footer updated to mention new affordances

3. **`58b599f` feat(search): `g <host>` jump pattern**
   - `parseJumpPattern` + `resolveJumpTarget` pure helpers
   - Search-input keydown listener catches `g <token>` + Enter, opens
     first matching clip in detail view
   - Tier order: exact > starts-with > contains; pinned nudge inside
     each tier; recency tie-break
   - Clears search box on success so list isn't stuck filtering on jump
     pattern after backing out
   - Cheatsheet row + placeholder hint added
   - **Sanity: 14/14** — parse pattern, resolve tiers, pinned nudge,
     no-match, hostless skip

4. **`e056689` feat(rules): live pattern-test panel in the rule form**
   - New pure `findCustomPatternHits(content, patterns)` in lib/util.ts
     returns non-overlapping merged ranges with `{ start, end, pattern }`;
     respects same 32-pattern + 200-char caps as `applyCustomPatterns`;
     zero-width regex guard prevents infinite loops; 5000-hit pathological
     guard per pattern
   - Collapsible `<details>` block in the rule form with sample-text
     textarea and `<pre>` result panel; each match wrapped in red span
     with tooltip showing matched pattern
   - Footer summary line: "N matches across M patterns · K invalid skipped"
   - Re-renders on every keystroke in either textarea + on load/reset
   - **Sanity: 23/23** — empty inputs, simple match offsets, disjoint
     matches, overlap merge (longer wins), invalid skip + counted,
     case-insensitive, zero-width safety, length cap, blank-pattern
     not-counted-invalid, 32-pattern cap

5. **`b2babea` feat(dedup): merge duplicates by content hash**
   - `mergeDuplicatesByHash()` in lib/db.ts: groups by hash, survivor =
     most-recently-seen, inherits sum(hitCount) + union(tags) +
     OR(pinned) + min(createdAt); losers go through trashClip soft-delete
   - Palette action "Merge duplicate clips by content" in Bulk group
   - `runMergeDuplicates` does pre-flight count + confirm dialog with
     concrete numbers; short-circuits with toast if 0 dupes
   - **Sanity: 25/25** — no-dup short-circuit, survivor selection,
     pinned OR-merge, multi-loser tag union + hit sum + earliest
     createdAt, hashless skip, mixed-groups counts

## Gates

- `tsc --noEmit` — clean
- `npm run build` — chrome ✅ firefox ✅ (~17ms / 7ms)
- All sanity suites:
  - export: 11/11
  - highlight: 9/9
  - import-dedup: 14/14
  - jump: 14/14 (new this tick)
  - merge-dupes: 25/25 (new this tick)
  - palette: 11/11
  - pattern-hits: 23/23 (new this tick)
  - sort: 13/13

## Push

`04f0f67..b2babea  feature/autoship -> feature/autoship` — landed.

## Deferred / nothing-to-flag

Nothing deferred; the 5 picked items were all ready and shipped clean.
Roadmap refilled in the same edit — collections, paste-stack, vault-
lock, image auto-recapture, inline diff, "Find duplicates" review
panel, list virtualization, right-click clip menu, etc.
