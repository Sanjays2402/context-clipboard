# Session 2026-06-28 12:44 PT — 5/5 shipped (frontend UX)

Batch loop tick. Frontend-only per Sanjay's standing override. Five
orthogonal slices across five distinct surfaces, each a pure module (or
thin HTML/CSS) + a thin wire. Gate green, pushed to main.

## Shipped (5/5)
1. `3f785bc` Send-to: word count in the single-clip Copy-weight row.
   lib/clip-weight now carries `words` (from the SAME computeContentStats
   the content-stats breadcrumb uses); summary reads "chars · words ·
   bytes", matching the breadcrumb's leading triple. 27/27 sanity-clip-weight.
2. `e3e8d90` Settings: live preview of the note caution-keyword tint. New
   pure lib/note-tint-preview paints stub note rows; flagged ones carry
   the warm tint + a keyword tag, verdict DELEGATED to lib/note-warning
   (the live detector) so it can't drift from the palette. 29/29 (new file).
3. `b62f806` Cheatsheet: "Detail header" group naming the 8 icon-only
   glyphs (Redact/Scrub/Archive/Send/History/Lock/Pin/Delete) as .cheat-act
   pills. HTML-only. 27/27 sanity-cheatsheet-detail (new; asserts each glyph
   maps to a real id="detail-" button — 8<->8, no phantoms).
4. `c5010ec` Note-composer: soft-red textarea border echo on a flagged
   note. Toggled off the SAME noteWarnBanner().flagged verdict that drives
   the banner. CSS + one-class toggle; verdict covered by its 27/27 sanity.
5. `e445b88` Bulk-bar: >1MB byte-budget heads-up on bulk Copy. New pure
   BULK_COPY_BUDGET_BYTES + exceedsCopyBudget + appendCopyBudgetWarning;
   the warning helper layers onto BOTH copy paths' toasts (string-generic)
   so the formatters + their row-count tests stay untouched. 21/21 (new);
   bulk-clipboard regression 64/64.

## Gate
- tsc --noEmit clean.
- chrome + firefox builds green (popup 507.8KB, bg 48.0KB, content 31.2KB).
- New/touched sanity: clip-weight 27, note-tint-preview 29, cheatsheet-detail
  27, copy-budget 21 (+ note-warn-banner 27 unchanged, drives slice 4).
- Neighbour regression green: send-to 144, json-line 45, content-stats 74,
  note-near-cap 45, cheatsheet-bulk 22, cheatsheet-settings 22,
  cheatsheet-filter 27, bulk-clipboard 64, bulk-md-chars 15,
  bulk-export-title 15, bulk-preview 30.
- Pushed 6b73a1d..e445b88.

## Deferred
None — all 5 are solid, demoable, revertible slices.

## Notes
- Surface spread: send-to, settings, cheatsheet, note-composer, bulk-bar —
  no single surface dominant, per recent tick discipline.
- Two slices reuse the SAME pure detector/state the live surfaces already
  run on: the note-tint preview delegates to lib/note-warning; the border
  echo (slice 4) rides noteWarnBanner().flagged. New surfaces can't drift.
- The >1MB warning helper is generic over the toast STRING (not the plan),
  so one helper covers both batch-copy buttons without touching either
  formatter or its existing tests.
- Roadmap refilled (+15 fresh frontend items, 12:44 PT block).
