# 2026-06-21 05:58 PT — 5/5 shipped

## Features

1. **Bulk archive/unarchive all filtered** (5b35feb)
   - New `archiveAllFiltered(boolean)` mirrors `pinAllFiltered`.
   - Two Cmd+K palette commands ("Archive all N filtered" /
     "Unarchive all N filtered"), gated on whether any
     unarchived/archived clips are visible.
   - Per-clip privacy audit entries written for every flip.

2. **Audit log filter chips in Settings** (d7b7e1a)
   - Bucket pills: All / Redact / Scrub / Lifecycle / Host / TTL.
   - Stale-filter guard auto-snaps to All on bucket emptying.
   - Summary line reflects active filter ("5 of 18" vs "18 actions").
   - 30/30 sanity (`.cron-state/sanity-audit-filter.mjs`).

3. **Audit log in JSON export bundle** (91ce7ec)
   - `exportAll()` attaches `privacyAudit` (newest-first; omitted
     when ring is empty).
   - `importAll()` union-merges by id, drops unknown/invalid shapes,
     caps at PRIVACY_AUDIT_MAX (30). Return shape gains
     `auditMerged`; popup toast + audit-section re-render show it.
   - 20/20 sanity (`.cron-state/sanity-audit-export.mjs`).

4. **Note composer pulls tags from active tab** (ced3d02)
   - New `src/lib/context-tags.ts` — pure helpers.
   - Strip now has "From this tab" (blue chips) + "Recent" segments,
     overlap-pruned.
   - 32/32 sanity (`.cron-state/sanity-context-tags.mjs`).

5. **Detail-view "Send to…" sub-menu** (8db76f3)
   - New `src/lib/send-to.ts` — 6 builders (open / google / site /
     mailto / md-link / fenced code).
   - Floating dropdown under detail action row; Cmd+K palette
     entry also surfaces it.
   - New `send` icon in lib/icons.ts.
   - 45/45 sanity (`.cron-state/sanity-send-to.mjs`).

## Quality gate

- `tsc --noEmit` clean.
- `npm run build` chrome + firefox green.
  - popup.js  171.9 KB
  - background.js 42.6 KB
  - content.js 23.8 KB
- All 16 sanity suites pass (11 archive + 20 audit-export +
  30 audit-filter + 32 context-tags + 11 export + 15 find-dupes +
  9 highlight + 14 import-dedup + 14 jump + 25 merge-dupes +
  11 palette + 23 pattern-hits + 15 privacy-audit + 45 send-to +
  13 sort + 11 templates + 22 lang-detect + 8 palette-host-boost +
  9 retro-redact + 9 palette-last-q + 16 site-rule-scrub +
  crypto + redact + popup-encrypt).
- Pre-existing playwright `test-redact-ui.mjs` DB-version mismatch
  unrelated to this batch.

## SHAs landed on `origin/main`

```
8db76f3 feat(detail): Send to… sub-menu — share, search, mail, md-link
ced3d02 feat(notes): tag chips pulled from active tab URL+host
91ce7ec feat(export): include privacy audit log in JSON bundle
d7b7e1a feat(audit): filter chips by category in Settings
5b35feb feat(archive): bulk archive/unarchive all filtered
```

## Deferred

None — 5/5 genuine. Roadmap refilled with 13 new follow-ons
(send-to extensions, audit log per-entry jump, bulk archive
preview, etc.).
