# Session 2026-06-27 15:36 PT — 5/5 shipped (frontend UX, fresh surfaces)

Steered away from the calendar/lightbox cluster of the last ~6 ticks onto
five orthogonal surfaces, each closing a real loop a prior tick left open.

## Shipped (5/5) — pushed 3091e8b..e602a2d on main

1. `86f5e47` feat(note-composer): live caution-warning banner while typing
   - New lib/note-warn-banner (pure). DELEGATES detection to lib/note-warning
     (hasNoteWarning + firstWarningKeyword — the exact gate the in-page
     palette row-tint runs on) so authoring preview + paste-time tint can
     never drift. Banner names the matched keyword, informational only
     (never blocks save), hidden for plain notes. HTML row + warm-red CSS;
     wired into openNoteComposer + the textarea input event.
   - 27/27 sanity-note-warn-banner.

2. `14d1228` feat(bulk-bar): byte weight alongside chars on Copy hover + toast
   - planBulkCopy now computes UTF-8 bytes of the exact joined payload.
     Hover "Copy 3 clips as text (1,240 chars . 1.2 KB)", toast "...- 1.2 KB".
     New utf8ByteLength + formatCopyBytes mirror lib/bulk-export byte-for-byte.
     No popup wiring change (plan already passed whole to both formatters).
   - 64/64 sanity-bulk-clipboard (was 51): emoji 4B / CJK 3B > chars,
     formatCopyBytes tiers, every toast/title/shape assertion updated.

3. `fc200a6` feat(trash): retention-urgency tint on the "X left" countdown
   - New lib/trash-ttl (pure, now-injectable). Tiers the trash purge
     deadline like the detail TTL banner: normal (>=2d, unchanged muted
     "Xd left") / soon (<48h amber) / imminent (<24h soft-red, HOUR grain
     "4h left") / expired ("Purges any moment"). Tail in a tier-classed
     span; imminent/expired rows get a whisper of warm bg. Day grain
     byte-identical to the old ceil math; defensive fallback preserves prior.
   - 31/31 sanity-trash-ttl.

4. `9ef3172` feat(search): is:expired operator + danger quick chip + Cmd+K
   - Strict already-past-due subset of is:expiring (expiresAt <= now).
     Parser bit + applyQuery predicate vs an injectable `now` (rolling
     Date.now() live); describeQuery; new tone-danger quick-chip style
     (soft-red resting, solid-red active) "Expired (N)" only when past-due
     clips exist; Cmd+K command + empty-state hint + cheatsheet row.
   - 26/26 sanity-is-expired.

5. `e602a2d` feat(cheatsheet): dedicated Calendar buckets + Lifecycle/TTL sections
   - Split the dense six-operator calendar cram-row into one row per
     operator + a Lifecycle group (is:expiring/expired/template/locked/
     noted, each naming its inverse). Live filter now isolates each
     operator; ArrowDown nav + "N of M" count pick the rows up free.
   - 27/27 sanity-cheatsheet-filter (was 17): per-operator findability +
     substring isolation.

## Gate
- tsc --noEmit: clean.
- npm run build: chrome + firefox both green (popup 496.4KB, bg 48.0KB, content 31.2KB).
- New + touched sanity: 175/175 (27+64+31+26+27).
- Full sweep: 135/135 sanity suites + 11/12 script tests pass.
  The 1 = the long-standing pre-existing playwright redact-ui IDB v3-vs-v4
  mismatch (every prior tick notes it; unrelated to this batch).

## New modules
- src/lib/note-warn-banner.ts
- src/lib/trash-ttl.ts
(plus extensions to bulk-clipboard.ts + search.ts)

## Deferred
None — all 5 are solid, demoable, revertible slices. Roadmap refilled
with 15 fresh frontend items spanning note-composer / bulk-bar / trash /
search / detail / settings / cheatsheet (mostly follow-ups from the loops
this tick opened: byte parity on the MD-copy + export paths, trash sort by
runway, the note-warn border echo + per-host keyword list, etc).
