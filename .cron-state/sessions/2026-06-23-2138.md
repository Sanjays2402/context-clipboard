# Tick 2026-06-23 21:38 PT — 5/5 shipped

Theme: a complete hashtag-cleanup affordance lattice. Five
orthogonal entry points for the same underlying primitives
(extract / merge / strip / sanitize) so per-clip + bulk + search +
palette-filter all stay in semantic lockstep.

## Shipped

1. **`22737d9` feat(search): is:hashtags / is:nohashtags**
   - Search operators in lib/search.ts
   - Narrows is:noted to clips with inline #hashtag tokens
   - Composes extractHashtagsFromNote (single source of truth)
   - 2 Cmd+K mirrors + empty-state hint string
   - Semantic axes documented: is:hashtags implies is:noted;
     is:nohashtags does NOT imply is:nonoted (prose-only pass both)
   - 19/19 sanity covering parser, applyQuery, describeQuery,
     composition with other operators, defensive shapes, grammar
     edge cases (foo#bar / URL fragments rejected), idempotence

2. **`6afdd19` feat(detail-view): per-clip "Strip N #tags" chip**
   - New pure module lib/note-hashtag-strip.ts
   - stripHashtagsFromNote (4-pass tidy preserving prose)
   - noteHasStrippableHashtags / countStrippableHashtagsInNote
   - 3 formatters (label/tooltip/toast) with count grammar
   - Detail-view chip sibling to promote chip, warm-neutral tint
   - Live refresh on all 5 paint anchors
   - 49/49 sanity: defensive shapes, grammar (incl. mid-token /
     URL-fragment rejection), idempotence, multi-line prose
     preservation, count + predicate, formatters at every shape,
     realistic end-to-end including em-dash prose

3. **`598f678` feat(bulk-bar): "Strip hashtags from notes"**
   - New pure module lib/bulk-strip-hashtags.ts
   - planBulkStripHashtags / perClipActionForStrip / isActionable
   - Toast + button-title formatters with emptiesNote tail
   - New scissors icon in lib/icons.ts (distinct from eraser)
   - Bulk-bar HTML wiring + visibility/title in updateBulkBar
   - No confirm (prose-preserving contract)
   - Cmd+K mirror under "Bulk" group
   - 38/38 sanity: defensive (null/empty/malformed), per-clip
     action shape with emptiedNote flag, plan aggregation,
     predicate, toast at every shape (1/M modified, with/without
     emptied tail), button-title formatter, realistic 5-clip
     end-to-end + bulk idempotence

4. **`2b4eedc` feat(detail-view): "Promote N #tags + strip" combo**
   - New pure module lib/note-promote-strip.ts
   - Single-click variant of two-step promote-then-strip workflow
   - Same gate as standalone promote (at least 1 new hashtag)
   - planPromoteAndStrip composes mergedTagsForClip +
     stripHashtagsFromNote — byte-identical to two-click path
   - 3 formatters with emptiesNote tail handling
   - HTML chip + accent-soft body / warm-border CSS bridge
   - Wired through all 5 paint anchors PLUS the standalone
     promote and strip click handlers (all three repaint in
     lockstep on every action)
   - 45/45 sanity: defensive shapes, plan composition (5 cases
     incl. gate, mixed, standalone-empties, case-insensitive),
     predicate, three formatters at every shape (5 label cases,
     3 tooltip variants, 7 toast shapes including cleared-tail),
     realistic em-dash end-to-end, mergedTags order preservation

5. **`8d30c7c` feat(palette): dynamic per-hashtag filter rows**
   - Extends lib/hashtag-discovery with hashtagFilterActionFor
   - Top-8 cap (full report still in discovery toast at top-12)
   - Each entry becomes a palette row with "Filter to clips with
     #X in notes (N clips)" label + contextual hint
   - alreadyTagged flag shapes label tail / hint / keywords
   - Composes is:hashtags operator (from feature 1) + literal
     #tag free-text needle for precision
   - 22/22 sanity: defensive (null/empty/non-string tag),
     standard + 1-clip + alreadyTagged labels, numeric clamping
     (NaN/negative/garbage), end-to-end with discovery pipeline
     producing 3 sorted action shapes, hyphenated tag pass-through

## Gates

- `tsc --noEmit` clean
- `npm run build` green for both chrome + firefox
  - popup.js: 391.7kb (+17.7kb vs 374.0kb baseline, 5 new modules)
  - background.js: 48.0kb (unchanged)
  - content.js: 31.2kb (unchanged — no content-script touches)
- All new sanity scripts: 173/173 pass (19 + 49 + 38 + 45 + 22)
- Zero regressions on 6 adjacent existing suites (hashtag-
  discovery, tag-from-notes, note-hashtag-promote, tag-from-
  notes-clear, clip-note, note-warning)

## Pushed

`git push origin main` succeeded: 22c122f..8d30c7c → origin/main
5 commits, each independently revertible:
- 22737d9 is:hashtags / is:nohashtags operators
- 6afdd19 per-clip Strip N #tags chip
- 598f678 bulk Strip hashtags from notes
- 2b4eedc per-clip Promote + Strip combo chip
- 8d30c7c dynamic palette per-hashtag rows

## Deferred / open follow-ups

None deferred from the 5-target. Added 5 follow-up roadmap items
for next ticks:
- Promote+Strip combo: "Promote N + CLEAR note" destructive variant
- Bulk Strip: storage-delta hint in bulk-bar
- is:hashtags counter chip on the quick-filter chip row
- Cmd+K hashtag rows: alpha + count-desc sort toggle
- hashtag-filter-action: bare-tag fuzzy-match enhancement
