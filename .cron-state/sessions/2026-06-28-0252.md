# Session 2026-06-28 02:52 PT — 5/5 shipped (frontend UX)

Batch tick. Closed five open loops from the 21:02 tick's follow-up list,
each on a distinct surface. All frontend per the standing override. Two
slices also unified a drifted second surface onto the shared helper.

## Shipped (5/5) — pushed 0837254..c300868 on main

1. `198c077` feat(note-count): graduated amber near-cap tier + unify
   detail editor onto it. NEAR_CAP_RATIO (0.9), nearCap predicate (warn-
   zone-but-safe, excludes over region), single `tier` verdict so the
   popup toggles ONE class. Composer foot reads tier; the DETAIL note
   editor's inline `len > CLIP_NOTE_MAX_LEN` string rewired onto
   noteCountState too — no drift, amber for free on both. New .near-cap
   CSS (accent-strong). 28/28 sanity-note-near-cap.

2. `d3d5466` feat(send-to): Copy weight (chars + bytes) row. New pure
   lib/clip-weight: chars = code points (computeContentStats), bytes =
   UTF-8 via the SAME utf8ByteLength the bulk receipts use. Summary IS
   the WYSIWYG clipboard payload. Null gate hides images + empty. kind:
   copy → generic dispatch, zero popup wiring. 22/22 sanity-clip-weight.

3. `fe4343e` feat(detail): pin-rescue cue on imminent + soon TTL tiers.
   Only expired told the user the way out. New TTL_RESCUE_HINT echoed in
   both detail lines ("at 4:32 PM — pin to keep it"); labels unchanged;
   popup already renders state.detail. Replaced the stale self-testing
   sanity-ttl-banner.mjs with sanity-ttl-rescue.mjs (bundles LIVE). 20/20.

4. `047d417` feat(bulk-bar): pre-commit byte receipt on Export hover.
   formatBulkExportButtonTitle (count + UTF-8 size + tagged grammar
   mirroring the toast). updateBulkBar serializes the visible (tag-
   filtered) selection once via the SAME bulkExportJson + utf8ByteLength
   the click uses → hover == what a click writes. tag-input listener
   live-refreshes. 15/15 sanity-bulk-export-title (real-serialization
   parity check included).

5. `8f20050` feat(cheatsheet): "Bulk actions" group. The ~14-button bulk
   bar had ZERO cheatsheet coverage. New flat accent .cheat-act pill
   (left-aligned, distinct from raised <kbd>) so they read as toolbar
   buttons; live filter + roving-focus pick them up free. 22/22
   sanity-cheatsheet-bulk (parses REAL popup.html + live filter module).

## Gate
- tsc --noEmit: clean.
- npm run build: chrome + firefox green (popup 503.0KB, bg 48.0KB, content 31.2KB).
- 107/107 new-feature sanity (28+22+20+15+22).
- Regression sweep (bulk-clipboard 64, bulk-export 87, cheatsheet-filter 27,
  content-stats 68, table-row 49, curl-note-comment 46, send-to-reorder 18,
  trash-ttl 31, is-expired 26, widen-bucket 34, density-preview 20, +more):
  all green.

## Test catch-ups (test-only, .cron-state/)
- `8414a69` sanity-send-to: row-count 17→18 + 3 weight-row checks → 144/144.
- `c300868` sanity-json-line: row-count 17→18 → 45/45.
(Both hard-asserted buildSendActions total; the new weight row grew it.)

## Deferred
None — all 5 solid.

## New modules
- src/lib/clip-weight.ts (new)
(note-count.ts, ttl-banner.ts, bulk-export.ts extended; detail note counter
unified onto note-count; cheatsheet HTML/CSS group added.)

## Push
0837254..c300868 → origin/main, verified via git fetch + log.
