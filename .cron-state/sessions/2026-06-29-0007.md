# Tick 2026-06-29 00:07 PT — 5/5 frontend

All work pushed to origin/main (dbf82c8 -> 9be42fd). Theme: content-aware
clip classification (code vs prose) wired through filters, chips, send-to.

1. cb5b011 — is:code / is:prose search filters. Reuse detectCodeLang +
   per-clip langOverride so the filter agrees with fenced-code/tint UI;
   images excluded from both → pair partitions text/link clips. Cmd+K
   commands + empty-state hint.
2. 1e56618 — "Code" quick-filter chip with live count, beside Templates.
   Uses codeMatches so chip total == filter; hidden on prose-only history.
3. 79a1428 — "Copy as CSV row" send-to (new csv-row module). RFC-4180
   quoting, same looksLikeTableRow gate as Markdown table-row.
4. 66745e4 — "Copy first sentence" send-to (new first-sentence module).
   Flatten newlines, terminator walk, initials guard, single-sentence hide.
5. 9be42fd — "Copy last sentence" send-to + is:code cheatsheet parity.
   lastSentenceOf shares first-sentence boundaries; pair surfaces together.

Send-to menu 25 -> 28 rows. Gate: tsc --noEmit clean, chrome+firefox builds
green (popup 523kb). Spot-checked sentence (initials/wrap/single) + csv
escaping in node strip-types. No deferrals. Roadmap refilled (+12 fresh).
