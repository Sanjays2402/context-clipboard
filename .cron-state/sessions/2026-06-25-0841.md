# Session 2026-06-25 08:41 PT

Batch tick — 5/5 shipped. Frontend focus override in effect (all slices UX).
Theme: orthogonal frontend papercuts — deliberately spread across list,
detail-stats, bulk-bar, footer, and detail-nav rather than clustering.
All five are open roadmap items (4 from the 2026-06-24 fresh-frontend
block, 1 from the 03:51 follow-ups).

## Shipped (in order, each its own commit)

1. `25d7fa7` feat(list): hover-peek tooltip for previews truncated at 140 chars
   - New pure lib/list-peek.peekTooltip: returns null when not truncated
     (no redundant tooltip), collapses interior whitespace to single spaces
     so multi-line bodies read as one paragraph, caps at 500 chars + trailing
     ellipsis, re-checks fit after flattening (whitespace-heavy => null),
     defensive on nullish + bad length opts.
   - Wired: renderClip computes peek for non-image clips, attaches as the
     .preview title (escaped). Images never get one.
   - Sanity: list-peek 21/21.

2. `8e65ea8` feat(detail-stats): Alt-click breadcrumb -> copy Markdown stat line
   - content-stats.formatContentStatsMarkdown: rebuilds from the same
     ContentStats as the plain path (bold figures only, not units), same
     hide rules + line-drop grammar. strip-** == plain invariant tested.
   - Wired: detailStats click handler branches on event.altKey (proper
     event param, NOT window.event); title advertises "Alt-click for
     Markdown"; MD path toasts "Copied as Markdown".
   - Sanity: content-stats 68/68 (was 39, +29 MD cases).

3. `c9812b7` feat(bulk-bar): count-aware Copy-selected previews joined char total
   - planBulkCopy gains chars = code-point length of the joined text
     (seams included) = exactly what hits the clipboard. Emoji = 1.
   - formatBulkCopyButtonTitle weaves it in with thousands grouping
     ("Copy 3 clips as text (1,240 chars)"); image-skip tail still follows.
     No popup change — title call site already passes a fresh plan.
   - Sanity: bulk-clipboard 50/50 (was 40, +10 char cases + shape update).

4. `78f3995` feat(footer): focus breadcrumb shows selection size during kbd nav
   - focus-position.formatFocusPosition gains optional selectedCount:
     positive finite -> "row 3 of 28 · 4 selected"; zero/missing/neg/NaN/
     Inf drops the tail. Composes with stale-index clamp; empty-list null
     still wins.
   - Wired: renderFocusPosition passes selectedIds.size (already repaints
     every render so the count stays live).
   - Sanity: NEW focus-position suite 26/26.

5. `4724a03` feat(detail): prev/next wraps around the filtered list + looped toast
   - New pure lib/detail-nav.nextDetailIndex(current, dir, total, wrap):
     {index, wrapped} for in-range or edge-wrap, null for nowhere-to-go
     (single/empty, bad inputs, edge step with wrap off). formatWrapToast
     gives direction-aware copy. Matches the similar-nav cycle that already
     wraps in this UI.
   - Wired: stepDetail list-mode branch wrap:true + toasts on wrapped;
     updateDetailNav keeps buttons enabled when >=2 clips (only single-item
     disables), pill title advertises the loop. Similar-nav mode untouched.
   - Sanity: NEW detail-nav suite 31/31.

## Gate (once, end of batch)
- `tsc --noEmit` — clean (EXIT 0)
- `npm run build` — chrome + firefox both green, popup 412.3kb
- All 5 sanity suites: 196/196 (21 + 68 + 50 + 26 + 31)

## Push
- 2c4ce0e..<tick-log> -> origin/main (verified after fetch).

## Notes / deferred
- Nothing deferred — all 5 genuinely solid + independently revertible.
- New libs: list-peek, detail-nav. Extended: content-stats (+MD),
  bulk-clipboard (+chars), focus-position (+selectedCount). All pure,
  zero network, MV3 + local-only intact.
- Roadmap still has open items in the current frontend block (search
  recent-dropdown, word-wrap per-clip override, copy-line-N, code
  soft-highlight, density radio, sticky day headers, chip-style tag
  editor) — above the <5 refill threshold, so no roadmap refill. Added
  5 fresh follow-ups spawned by today's work (peek URL tail, MD-stats
  palette command, char-total in toast, wrap opt-out toggle, selection
  overflow distinction).
- Deliberately skipped the riskier open items: chip-style tag editor
  (ripples through 6+ hashtag call sites), sticky day headers / copy-line-N
  (destabilize the flat-list keyboard model), density radio (changes the
  persisted Settings shape). Picked safe, additive, independently
  revertible slices instead — quality floor over hitting the number with
  churn.
