# Tick 2026-06-29 08:41 PT — 5/5 shipped (frontend UX, tabular cluster)

Continued the content-aware tabular classification cluster. All 5 slices
frontend, all demo-able, all on `main`, pushed + verified on origin.

## Shipped (5/5)

1. **e869e79** `feat(search): is:tabular operator (union of is:csv + is:tsv)`
   - lib/table-kind.ts: `tabularMatches(c) === looksLikeTableRow(c)`.
     Delegates straight to the gate (not csvMatches||tsvMatches) so the
     union can't drift from the csv/tsv partition — a future delimiter
     joining looksLikeTableRow folds in automatically.
   - search.ts: import, ParsedQuery.tabularOnly, parser init + branch,
     applyQuery gate, describeQuery bit. Mirrors is:csv/is:tsv (780671a).
   - popup.ts: "Show tabular clips" Cmd+K command + empty-state cheatsheet.

2. **e0c6e2b** `feat(chips): "Tabular (N)" quick-chip toggling is:tabular`
   - Single chip (not two CSV/TSV chips) — tighter for narrow popups.
   - tabularCount via tabularMatches; counted INDEPENDENTLY of code/prose
     (a tabular row also reads as prose), so it's an orthogonal cut, not a
     fourth partition — co-exists in the strip without double-claiming.
   - Hidden when zero, mirroring Code/Prose chip gates.

3. **9f92ff9** `feat(content-stats): column count segment for tabular clips`
   - content-stats.ts: `tabularColumns()` reuses looksLikeTableRow +
     splitTableCells, so "3 cols" == the cells Copy-as-CSV-row produces.
   - 2+ cells floor. Wired into plain + Markdown formatters (bold-figure /
     plain-unit), so md/plain parity (strip ** -> plain) holds.

4. **dd6c3f5** `feat(detail): code badge shows the detected language inline`
   - clip-kind-badge.ts: resolves via effectiveLang(langOverride,
     detectCodeLang) — names exactly the language the detail tint paints.
     "code · sql" / "code · rust". Bare "code" when no language resolves.
   - op stays is:code so click still filters; data-kind still drives accent.

5. **f75c772** `feat(list): faint tabular grid stripe on CSV/TSV row clips`
   - popup.ts: kindStripe — is-code wins, else is-tabular, else none
     (mutually exclusive; a single-line input could match both).
   - popup.css: .clip.is-tabular::before — success-green (vs code accent
     gold), opacity 0.3/0.65, same shape as the code stripe.

## Gate (once, end of batch)
- `tsc --noEmit` clean.
- `npm run build` green: chrome 536.9kb + firefox 536.9kb (was 534.9kb).

## Deferred (still open in the 05:10 refill cluster)
- Mini-table preview (real <table> toggle), Copy-as-Markdown-table-with-
  header, Copy column N picker, bulk Copy-as-TSV, tabular complement
  empty-state, prose ~min pill, code line-numbers gutter, Copy first N
  lines, longest-line stat, copy-as-JSON-array. Plenty of runway — next
  tick can take the mini-table preview + column picker (richer surfaces).

## Notes
- No new IDB schema, no network, MV3 intact, local-only intact.
- All five reuse existing single-source-of-truth predicates
  (looksLikeTableRow / splitTableCells / effectiveLang / codeMatches), so
  filter + chip + stripe + badge + cols can never disagree on a clip.
